@AGENTS.md

# World Cards

A cross-platform mobile platform for traditional card games (Expo/React Native/TypeScript), designed to scale to 100+ games over years. See the full product vision and constraints in the system-level project brief (also summarized in the design spec below).

## Architecture

Full architecture decisions live in:
- `docs/superpowers/specs/2026-07-06-core-architecture-design.md` — the approved design (repo structure, Card/Rule/AI engine contracts, state management, persistence, testing strategy, risks)
- `docs/superpowers/plans/2026-07-06-core-architecture.md` — the 17-task implementation plan that built it

**Repo structure:**
- `packages/engine` — pure TypeScript, zero React/React Native/Expo dependency. Card Engine, `RuleEngine`/`AIStrategy` contracts, game registry, persistence interface, statistics, and a reusable `simulateGames` test harness. Every game's rules/AI live here as pure functions over immutable, JSON-serializable state.
- `apps/mobile` — the Expo app. All RN/Expo/Reanimated/Zustand/React Navigation code lives here. Consumes `packages/engine` via an npm workspace link (`@world-cards/engine`).

**Key conventions established in Phase 1** (see the spec for full rationale):
- Engine is a pure functional core: no classes, no internal mutable state, no `Math.random()` (use the seeded `RNG` from `packages/engine/src/core/rng.ts`).
- Adding a new game = two new folders only: `packages/engine/src/games/<id>/` (rules + AI) and `apps/mobile/src/games/<id>/` (screen), registered via `registerGame`. No shared/shell code should need editing.
- Zustand: one small global store (settings) + one short-lived per-game-session store (via `createGameSessionStore`) — never one mega-store.
- Testing: Jest multi-project (`packages/engine` on Node, `apps/mobile` on jest-expo + RNTL). Every game should get simulation tests via `@world-cards/engine/testing`'s `simulateGames`, asserting card-conservation invariants over hundreds of simulated games.

**Testing policy (as of 2026-07-07, supersedes the general TDD default until revisited):** writing tests up front adds too much time to this project's pace. Default to *not* writing new tests, especially for mobile UI components/screens — ask before adding one. The exception is the engine core (Card Engine, `RuleEngine`/AI implementations, `simulateGames`-based invariant checks) — that stays test-covered by default, since correctness there is load-bearing for every game. Existing tests should still be run for regression-checking; this policy is about not writing new ones proactively.

## Card Playground (`apps/playground`)

A separate, isolated Expo app (sibling to `apps/mobile` in the same npm workspace) for prototyping card and table visual designs — border radius/color + overlay image per card group (number/face/ace), plus felt/wood-corner color for the table. Full rationale and data model: `docs/superpowers/specs/2026-07-12-card-playground-design.md`.

**Isolation is deliberate and load-bearing, not incidental:** `apps/playground` shares `packages/engine` (pure TS, zero RN dependency) and, as of 2026-07-14, `packages/ui` (`PlayingCard`, `TableFelt`, `TableWoodCorners`, `SuitIcon`, etc. — see `docs/superpowers/specs/2026-07-14-shared-ui-package-design.md`) with the rest of the repo. It never imports from `apps/mobile` directly, and `apps/mobile` never imports from or links to `apps/playground`, in development or production. Playground-only dependencies (file/image pickers, sliders) must only ever be added to `apps/playground/package.json`, never to `apps/mobile/package.json` or `packages/ui/package.json`.

**Standing rule: whenever a change is made to how cards or the table look in `apps/mobile` (or vice versa — a deliberate update to the playground's look), treat that as a trigger to explicitly discuss with the user whether/how the other side should be updated to match or extend.** Do not silently skip this conversation, and do not auto-mirror changes either — there is no automation or lint enforcing this, it is a reminder for every future session to raise it. The one exception is `packages/engine`: since `apps/playground` genuinely depends on it, breaking changes there must be checked against both `apps/mobile` and `apps/playground` as a normal correctness matter, not a discussion trigger.

## Status

**Phase 1 (core architecture): complete**, merged to `master`. Validated end-to-end against an internal test-only fixture game (`cardDraftGame`, in `packages/engine/src/rules/__fixtures__/`) — not a real game, just proof the shared contracts work. No real game exists yet.

**Phase 2 (Pişti): rules + state model + RuleEngine + AI + registration all complete**, merged to `master`. Only the mobile UI/screen remains. Per the project roadmap, games are built in this order: Pişti → Klondike Solitaire → Spider Solitaire → FreeCell → Hearts → Spades → Gin Rummy → Crazy Eights → Blackjack → Texas Hold'em. Each game follows: research rules → rules doc → game state design → rule engine → UI → Easy/Medium/Hard AI → tests → simulate hundreds of games → done, then move to the next game. (Pişti deliberately did AI before UI so `simulateGames`-based testing and `registerGame` — which requires `aiStrategies` — were unlocked earlier; see the AI-strategies spec below for the rationale.)

Pişti design/plan docs so far:
- `docs/superpowers/specs/2026-07-07-pisti-rules-and-state-design.md` — rules doc + `GameState`/`Move` data model
- `docs/superpowers/plans/2026-07-07-pisti-state-model.md` — implemented the `moveAllCards` core primitive + `PistiState`/`PistiMove`/`PistiSetupOptions` types
- `docs/superpowers/specs/2026-07-07-pisti-rule-engine-design.md` — `RuleEngine` file structure, `validateMove` semantics, test-coverage plan
- `docs/superpowers/plans/2026-07-07-pisti-rule-engine.md` — implemented `pistiGame: RuleEngine<PistiState, PistiMove>` in `packages/engine/src/games/pisti/rules.ts`
- `docs/superpowers/specs/2026-07-07-pisti-ai-strategies-design.md` — Easy/Medium/Hard AI approach, registration, `simulateGames` testing plan
- `docs/superpowers/plans/2026-07-07-pisti-ai-strategies.md` — implemented `pistiEasyAI`/`pistiMediumAI`/`pistiHardAI` (`packages/engine/src/games/pisti/ai/`), registered `pistiDescriptor` (new `'fishing'` `GameCategory`) via `packages/engine/src/games/pisti/index.ts`

Pişti is now registered: `getGame('pisti')` returns a full `GameDescriptor` with all three AI difficulties. `simulateGames`-based tests pass (500 easy-vs-easy games with no card-conservation violations; Hard beat Easy 154/200).

Pişti UI design/plan docs:
- `docs/superpowers/specs/2026-07-07-pisti-ui-design.md` — navigation, screen/component breakdown, state flow, testing plan
- `docs/superpowers/plans/2026-07-07-pisti-ui.md` — implemented the full playable slice: `apps/mobile/src/components/{PlayingCard,GameScreenLayout,GameResultModal}.tsx` (shared, reusable by future games), `apps/mobile/src/games/pisti/{PistiSetupView,PistiTable,PistiScreen,useAITurn}.tsx`, `apps/mobile/src/games/registry.ts`, and a new `@world-cards/engine/games/pisti` subpath export

Pişti is now fully playable end-to-end: Home → pick a difficulty → play a full hand against Easy/Medium/Hard AI → see the result → play again or return home. `useAITurn` uses a thinking-delay `setTimeout` to pace AI moves (the `InteractionManager.runAfterInteractions` wrapper it originally used was dropped during the polish pass below — RN's InteractionManager is now a deprecated no-op stub). **Frame timing has still not been profiled on a real mid-range Android device** — this remains the standing Phase 1 performance risk and should happen before Hard AI is trusted in more complex future games.

Deliberately deferred from this UI sub-project (see the design doc for rationale): Reanimated/animated transitions, save/resume via `PersistenceAdapter`, `stats:<gameId>` recording, multi-hand match play, the 4-player/2v2 variant, human-vs-human pass-and-play, and lifting `useAITurn` into a shared cross-game hook (revisit once Hearts is built). `GameResultModal`'s tests also have a known, deferred `act()` warning from the Modal's fade animation — not fixed in this pass; the correct fix if revisited is `animationType="none"` instead of `"fade"`.

Manual smoke test in Expo Go passed (2026-07-07) — the game is playable end-to-end and feels good.

**Priority change (2026-07-07):** before starting Klondike Solitaire, finish polishing Pişti to "flawless." Klondike does not start until this polish pass is done. Polish work in flight/complete:
- Stacked/stepped pile rendering in `PistiTable` (cards no longer render exactly on top of each other)
- Removed the deprecated `InteractionManager` usage from `useAITurn` (it's now a no-op stub in the installed RN version anyway)
- Expo/dependency warnings cleared via `npx expo install --check`
- Visual polish: `HomeScreen` gaming-vibe restyle, green felt background on the Pişti screen
- `packages/engine/tsconfig.json` moved off the deprecated `moduleResolution: "node"` (node10) to `"nodenext"` (+ `isolatedModules: true`), fixing the TS6-deprecation warning ahead of TS7
- Table UI overhaul (2026-07-08) fixing three usability bugs found in manual play: low-contrast text on the new green felt (pile count/banner/title recolored), the pile visibly shifting position when the hand row emptied (opponent/hand rows now have fixed-height slots so the center pile area never moves), and the opponent's move being invisible (a ~550ms "reveal" beat now shows the AI's chosen card highlighted in the pile before it's committed to state, via `revealedMove` state + a nested `setTimeout` in `PistiScreen`, not a second effect-scheduled timer — that approach broke under fake timers, see `PistiScreen.tsx`). Also added a felt "mat" under the pile and per-player captured-card-count badges.
- Second UI polish pass (2026-07-08): `PlayingCard` redesigned with corner rank/suit + a blank `centerArt` placeholder view marked for future card artwork. Hand layout split by role — local player is a plain evenly-spaced face-up row, opponent is a face-down fan (rotation + quadratic `translateY` curve, center card highest) since a flat overlap read as messy for a face-down hand. Added tap-to-select-then-tap-to-play card interaction via two new reusable pieces in `apps/mobile/src/components/`: `useCardSelection` (selection state machine) and `SelectableCard` (lift/highlight animation), intended for reuse by every future game's hand UI, not just Pişti.
- Four-player mode (2026-07-08): Pişti now supports 1 human + 3 AI, individual free-for-all (no teams), as a player-count choice on the setup screen alongside the existing 2-player game — 2-player behavior is unchanged (verified: every pre-existing rules/simulate test still passes untouched). This was a genuine N-player generalization, not a hardcoded 4p branch:
  - `packages/engine/src/games/pisti/rules.ts`: `PistiSetupOptions.players` is now `PlayerId[]` (was a 2-tuple); `setup`/redeal deal to every player generically; `calculateScore`'s majority-capture +3 bonus now goes to the sole leader across all players (no bonus on any tie, matching the original 2-player rule). Confirmed via new `rules.test.ts` cases and a 500-game 4-player `simulateGames` run with zero card-conservation violations.
  - AI strategies (`easy`/`medium`/`hard`) needed **no changes** — all three already operated generically over `state.players`, including Hard AI's minimax `evaluate` (sums all opponents' scores, not just "the" opponent). `maxDepth: 8` is unchanged for 4p, which now covers only 2 full rounds instead of 4 — not a correctness issue, just a possible future tuning point if Hard AI feels weaker at 4p.
  - `apps/mobile/src/games/pisti/useAITurn.ts`: takes `aiPlayerIds: PlayerId[]` instead of a single id, and reports which player moved (`onMove(move, playerId)`) so the reveal-beat logic in `PistiScreen` works for any AI seat, not just one.
  - `apps/mobile/src/games/pisti/PistiTable.tsx`: seats are now data-driven (`assignSeats`) — 1 opponent sits at the top (unchanged 2p look), 3 sit left/top/right. Side seats show a vertical card stack rather than a rotated horizontal fan — RN transforms don't affect layout sizing, so a 90°-rotated fan would've kept its wide unrotated footprint reserved in the row, risking overflow on a phone-width screen; deliberately chose the safer option since this can't be visually verified in this sandbox.
  - Setup screen (`PistiSetupView`) gained a 2/4-player picker above the difficulty picker; all 3 AI opponents share one difficulty (no per-seat difficulty), per your call.
- Counter-clockwise turn order + partner (2v2) mode (2026-07-08):
  - **Counter-clockwise fix**: seating was accidentally clockwise (bottom→left→top→right). `PistiTable.assignSeats` now maps turn order to bottom→right→top→left, matching `state.players` order (human, then AI seats in turn order).
  - **Partner mode**: added as a second axis alongside player count — the 4-player setup screen now also asks "Free-for-all" vs "With a Partner". Engine-level: `PistiSetupOptions`/`PistiState` gained an optional `teams: PlayerId[][] | null` field; `calculateScore` was refactored around a "scoring unit" concept (a team's pooled members, or a lone player in free-for-all) so both modes share one code path — free-for-all is `units = players.map(p => [p])`, which reduces to the exact old per-player math (verified: all pre-existing tests pass untouched). Partner is always whichever AI seat sits across the table (top seat, second in turn order) — geometrically unambiguous since there's only one human. Verified with a 500-game team-mode simulation asserting teammates always finish with equal pooled scores.
  - **Caught a real correctness trap**: `GameResultModal` computed "is this a tie" from `winners.length > 1`. In team mode a clean team win naturally produces 2 winners (both teammates share the pooled score), which would have wrongly shown "It's a tie!" instead of "You win!". Fixed by counting *distinct teams* among the winners instead of raw winner count (`teams` is an optional prop, so free-for-all behavior — where every winner is its own "team" — is unchanged).
  - AI strategies again needed no changes — teams only affect scoring, not legal moves or card play.
- **Not yet visually verified on a native device** — the 2-seat polish, 4-seat layout, and counter-clockwise/partner seating from the bullets above predate the browser-verification workaround below and still haven't been screenshotted; needs a manual check in Expo Go or the browser before considering this "flawless." (Reminder for future sessions: the user said they'll set up adb/emulator/browser access later — keep flagging the *native* gap even once browser checks are routine.)
- **Browser-based visual verification unlocked (2026-07-08):** installed `react-native-web`/`react-dom` so `expo start --web` runs, plus `playwright-core` driving the sandbox's already-installed system Chrome (`executablePath`, no bundled browser download needed) to take real screenshots. Not a substitute for a native device pass, but a real step up from zero visual verification — see `[[dev_sandbox_no_device_access]]` memory. First use of this workflow immediately caught a real bug: `PistiSetupView` (the table-size/difficulty picker) was completely unstyled (default RN grey), and — more seriously — had no way back to Home at all (`Game` stack has `headerShown: false` and the view wasn't wrapped in `GameScreenLayout`). Fixed: themed to the app's navy/gold tokens and given a working `onBack` header link; `PistiSetupView.test.tsx` updated (not expanded) to pass the new required prop. Lesson written to memory: this changelog records *intent*, not *verified rendered state* — don't trust "polish pass" narrative entries as visual proof without an explicit screenshot mention.
- **Alper Games (Batak HD / Spades Batak) adopted as the explicit visual reference** for the Pişti table (2026-07-08), at the user's direction — three reference screenshots checked into `docs/references/pisti/` (see its README for sources/attribution). Published as a Claude Artifact checklist ranking the gaps cheapest→biggest lift: card center art, felt texture/vignette, active-turn glow on the player badge, card back pattern, wood-grain table frame, per-seat avatars, per-seat trick layout (played cards near the player who played them, not merged into one pile — biggest lift, do last). Explicitly out of scope: coin/stake badges, chat/emote, and the suit-selector bidding UI — all tied to Batak features (wagering, human social play, trump bidding) Pişti doesn't have.
- **Card center art shipped (2026-07-08), first item off that checklist.** Rejected both a literal port of the Batak reference art (illustrated court-card portraits, traditional pip layouts) and a "just enlarge the Unicode glyph" patch — at this component's actual render size (56×80 normal / 36×52 small), pip layouts read as cluttered noise and illustrated portraits would blur, while Unicode suit glyphs (`♥♦♣♠`) render inconsistently across platforms/fonts. Instead added `apps/mobile/src/components/SuitIcon.tsx` — hand-authored SVG paths (via new `react-native-svg` dependency, installed through `npx expo install`) for all four suits, reused for **both** the corner index and a new large, low-opacity (`0.16`) centered "watermark" suit shape in `PlayingCard.tsx`'s previously-empty `centerArt` placeholder. Same treatment for every rank (no per-rank pip counting) — deliberately deferred illustrated court-card art as a separate, bigger future investment (real art sourcing/curation, not a quick patch). Considered and declined AI-image-generated art for this pass: stylistic consistency across a full suit/rank set requires a curation pipeline (transparent-background cleanup, palette matching) that isn't worth it for four simple geometric shapes; flagged as worth reconsidering specifically for a future illustrated-court-card project. `PlayingCard.test.tsx` updated (not expanded) to query the new `corner-suit-<suit>` testID instead of the old Unicode text. Visually verified via the browser workflow above — screenshots confirm all four suit icons render crisp and legible at both corner and watermark size, no console errors.
- **Felt texture/vignette and active-turn badge glow shipped (2026-07-09), items 2–3 off the checklist.** Both scoped entirely to `apps/mobile/src/games/pisti/PistiTable.tsx` (no shared-component changes). Felt texture: a static `FeltTexture` overlay (`react-native-svg` `Pattern` for a faint diagonal diamond weave + `RadialGradient` for edge-darkening vignette), absolutely positioned behind all table content, `pointerEvents="none"`, painted once and never re-rendered by game state — zero runtime cost during play. Active-turn indicator: replaced relying solely on the existing whole-area gold wash (`activeArea`, kept as-is for the "these cards are tappable" affordance) with a dedicated pill-shaped `badge` wrapping each player's name label; `badgeActive` adds a bright green border + shadow-based glow (`#4ade80`) matching the third reference screenshot's "glow ring around the whole badge," clearly distinguishable from the general gold tint. Visually verified: felt weave and vignette are visible but subtle, the active player's badge glows green while the inactive one stays a plain dark pill. Full mobile test suite (38 tests) still passes untouched — no test asserted on the unwrapped label markup.
- **Card back pattern shipped (2026-07-09), item 4 off the checklist.** Scoped to `apps/mobile/src/components/PlayingCard.tsx` (the shared card component, not table-specific). Deliberately did not copy the reference's red rosette literally — recolored to the app's own navy/gold identity (`back` bg changed from a generic mid-blue `#2f5fa8` to a deep navy `#1c2451`) instead of importing Alper Games' brand color. New `CardBackPattern` (`react-native-svg`) layers a faint gold diagonal-diamond lattice, a thin inset gold frame, and a simple centered gold lozenge emblem — same "keep it simple at 36–56px" reasoning as the center-art decision: an ornate rosette would blur at this render size, so the emblem is a plain geometric shape, not per-suit/per-rank detail. `back` style gained `overflow: 'hidden'` so the pattern clips to the card's rounded corners. No test changes needed (no test asserted on back-face internals beyond the existing `playing-card-back` testID). Visually verified via a close-up crop of the opponent's fanned hand — frame, lattice, and emblem all read cleanly at actual card size, no console errors.
- **Performance pushback + `React.memo` pass (2026-07-09).** You raised a valid concern: `PlayingCard`/`PistiTable` are shared across every future game, so the SVG additions above need to stay light. Assessment: architecturally cheap (native vector draws, no images/network, ~25-30 small SVG instances even on a 4-player table, only redrawn on discrete move events since the reveal/capture animations already run on the native thread via `useNativeDriver: true`) — but this is reasoning, not a measurement, since there's still no on-device profiling capability in this sandbox. Concretely, not just reassurance: wrapped `SuitIcon`, `CardBackPattern`, `PlayingCard` itself, and `FeltTexture` in `React.memo` (nothing in the app was memoized before this). Confirmed the engine reuses `Card` object references across zone-array rebuilds (`packages/engine/src/core/table.ts`'s `[...cards]` spreads don't clone individual cards), so `PlayingCard`'s memo on the `card` prop is a real win for the ~20+ cards that didn't move on a given turn, not a no-op. See `[[pisti_card_ui_perf]]` memory for the full reasoning, including the one caveat (some `PistiTable` call sites pass a freshly-constructed `style` object each render, which defeats the outer `PlayingCard` memo specifically for those — left as-is since the inner `SuitIcon`/`CardBackPattern` memos still catch the real cost). Full mobile suite (38 tests) + typecheck both clean after the change; re-screenshotted to confirm zero visual regression.
- **`pointerEvents` prop deprecation fixed (2026-07-09).** You reported console warnings from clicking around the web preview. Triaged: three of the four were `react-native-web`-only noise (`textShadow*`/`shadow*` prop deprecation — the *correct* form on native, since real RN has no `boxShadow`; `useNativeDriver`/`RCTAnimation missing` — `react-native-web` has no native animation driver at all, the "run `pod install`" advice in that message is native-iOS boilerplate that doesn't apply here; and the `Web Bundled`/`Running application` lines, which are just normal Metro dev-server logs) — left alone since web isn't a real target for this app, just the sandbox's screenshot tool. The fourth (`props.pointerEvents is deprecated. Use style.pointerEvents`) was real and ours: `FeltTexture`/`CardBackPattern` used the `pointerEvents="none"` prop form instead of `style.pointerEvents`. Fixed in both; confirmed via a console-log capture that our code no longer emits it (one instance still fires from a third-party dependency, before Pişti even loads — not ours to fix).
- **`/simplify` cleanup pass (2026-07-09)**, prompted by you flagging that `PlayingCard`/`PistiTable` need to stay modular and DRY since every future game depends on them. Ran 4 parallel review agents (reuse/simplification/efficiency/altitude) over the uncommitted diff; findings converged strongly enough to act on:
  - **Killed real duplication**: `PistiTable.tsx`'s top/left/right opponent-seat blocks were the same ~15-line filter+map+derivation (handCount/isCurrentTurn/isRevealing) copy-pasted three times — `OpponentSeat` now takes `state`/`revealCard` directly and derives all of that internally once; call sites collapsed to a 3-line `OpponentSeatGroup` helper per layout slot. Also pulled the badge/label JSX (duplicated between opponents and the human row) into a local `PlayerBadge`.
  - **New shared components** (`apps/mobile/src/components/`): `AbsoluteOverlay.tsx` (the pointer-events-none absolute-fill wrapper `FeltTexture` and `CardBackPattern` had each hand-rolled separately, including a duplicated `noPointerEvents` style literal), `glowShadow.ts` (the "colored glow" shadow recipe `PlayingCard`'s `highlighted` style and `PistiTable`'s `badgeActive` style had each hand-rolled with different radii), `TableFelt.tsx` (promoted from `PistiTable`'s local `FeltTexture` — it was already zero-prop and Pişti-agnostic, and felt tables are near-universal for the games still on the roadmap, so extracting now beat waiting for a second consumer to copy-paste it), `CardBackPattern.tsx` (moved out of `PlayingCard.tsx` into its own file, matching the existing `SuitIcon.tsx` precedent).
  - **New `apps/mobile/src/games/pisti/pistiSeating.ts`**: pulled the pure, side-effect-free geometry (`assignSeats`, `fanRotationDeg`, `fanCurveY`, `SeatPosition`, the fan/stack overlap constants) out of `PistiTable.tsx` — no JSX/RN dependency, independently unit-testable, and it shrank the table file materially (**376 → 289 lines**; `PlayingCard.tsx` went **148 → 96** after the `CardBackPattern` extraction). Also exports a new `PARTNER_SEAT_INDEX` constant: `PistiScreen.tsx`'s `buildTeams`/`buildPlayerNames` used to hardcode `aiIds[1]` as "the partner" based on nothing but a *comment* pointing at `PistiTable.assignSeats` in a different file — a real "two places encode the same invariant with no shared source of truth" bug risk the altitude-review agent caught. Both now derive from the one constant.
  - **`PILE_CARD_OFFSETS`-style fix for `React.memo` effectiveness**: the side-seat card stack was passing a freshly-allocated `{marginTop: ...}` style object on every render, silently defeating `PlayingCard`'s memo from the previous entry — replaced with a precomputed `SIDE_CARD_STYLES` lookup array (same pattern already used for the pile stack) so the same object reference is reused across renders.
  - `rules.ts`'s `calculateScore` majority-bonus lookup simplified from a `reduce`+spread building an unused array of leader indices down to a plain `filter().length` + `indexOf()`. `PistiScreen.tsx`'s `buildPlayerNames` (session-constant inputs) wrapped in `useMemo` so it's not rebuilt every move.
  - **Explicitly skipped** (per the review agents' own judgment, not just mine): further splitting `PistiTable.tsx`'s `RevealCard`/`OpponentSeat` into separate files (each is small, used once, tightly coupled to this table's styles — splitting would trade "one file, clear sections" for "five files to jump between"); extracting the navy/gold color literals duplicated between `HomeScreen.tsx` and `PistiSetupView.tsx` into a shared theme module (real, but a bigger/riskier foundational decision than a cleanup pass should smuggle in — flagged for a future deliberate pass); precomputing the top-seat fan transform the same way as the side stack (depends on an (index, count) pair, low element count, not worth the added complexity given it's already a known, accepted tradeoff per `[[pisti_card_ui_perf]]`).
  - Verified: typecheck clean on both packages, full test suite **122/122 passing across 30 suites** (mobile + engine, including the 500-game team-mode simulation), and re-screenshotted both the 2-player table and — since `buildTeams` was touched — the 4-player "with a partner" table specifically, confirming Partner/Opponent 1/Opponent 2 seating is pixel-identical to before the refactor.
- **Wood-grain corner accents shipped (2026-07-10), item 5 off the checklist.** Brainstormed with the visual companion first: three frame styles were mocked (full picture-frame border, top/bottom rails only, corner wedges only) — you picked corner wedges (C), with badges staying separate/floating rather than nested into the wedge shape, and dark mahogany over amber oak for the wood tone. The other two styles are documented as deferred alternatives, not built as an unused variant system — mockup HTML persists under `.superpowers/brainstorm/` (gitignored) and the choice/rationale is recorded in `docs/superpowers/specs/2026-07-10-pisti-wood-corners-design.md`. New `apps/mobile/src/components/TableWoodCorners.tsx`: four quarter-circle wood wedges (react-native-svg gradient + faint grain pattern + thin gold `#ffd966` trim stroke) pinned at fixed 56dp size to the table container's four corners, following the exact `TableFelt`/`CardBackPattern` convention (zero-prop, `AbsoluteOverlay`-wrapped, `React.memo`'d) — rendered in `PistiTable.tsx` right after `<TableFelt />`, so same z-order, no seat-count dependency (identical for 2p/4p). Built via subagent-driven-development in an isolated sibling worktree (per `[[worktree_jest_nesting_bug]]`): implementer transcribed the plan's exact code, task review confirmed the SVG arc geometry is genuinely convex (not an accidentally-concave notch) across all four corners, and visual verification via the browser/Playwright workflow (`[[dev_sandbox_no_device_access]]`) screenshotted both the 2-player and 4-player tables — wedges render correctly, no overlap with the 4-player side-seat card stacks, no new console warnings. No new tests (pure decorative UI, per policy). Final whole-branch review: ready to merge, no critical/important findings (two minor, non-blocking notes: the grain texture is barely visible at this render size, and the wood/gold colors are hardcoded constants like `TableFelt`'s — both swept into the already-tracked future navy/gold theme-extraction pass rather than fixed here). Merged to master.
- **Per-seat player avatars shipped (2026-07-10), item 6 off the checklist.** Brainstormed with the visual companion again: glyph style (generic silhouette vs. a `SuitIcon`-monogram vs. an initial-letter monogram — silhouette chosen, literal match to the reference) and placement (avatar stacked above the badge like the reference vs. merged inline into it — inline chosen, since a stacked layout doesn't fit the 4-player mode's 64dp side-seat width budget). New `apps/mobile/src/components/PlayerAvatar.tsx`: small SVG person-silhouette icon in a circular ring, `React.memo`'d, following the `SuitIcon` convention — one prop, `accent?: boolean` (deliberately generic, not `isHuman`; the caller decides). Gold ring = a static "this is you" marker on the human seat only; every AI seat (however many, 1 or 3) renders an identical plain-ringed avatar — this is independent of the pre-existing green `badgeActive` active-turn glow, and both signals are confirmed simultaneously legible in screenshots. `PlayerBadge` in `PistiTable.tsx` now renders the avatar inline before the name/capture-count text, inside the same pill. Visual verification (browser/Playwright workflow) caught a real regression mid-task: the avatar broke the 4-player mode's 64dp-wide side seats, wrapping badge text onto 5 broken lines — root-caused to the avatar+gap+padding leaving too little width for text in that column. Fixed with a `size?: 'normal' | 'small'` prop on `PlayerAvatar` and a `compact` prop on `PlayerBadge`, applied only to the two side seats (top/human badges are pixel-identical to before the fix). **This is a deliberate, surfaced deviation from the original plan's "identical avatar size across every seat type" constraint** — shown to you as a concrete before/after tradeoff (screenshots of the broken wrap vs. the fix), you chose to keep the per-seat sizing over forcing one size everywhere. A follow-up task review caught a real Important gap in that fix (no text-truncation safeguard, only verified at capture counts 0 and 5, not the double-digit counts a real hand can produce) — fixed with `numberOfLines={1}` on the badge label, re-reviewed clean. No new tests (pure decorative/presentational branching, same policy as `CardBackPattern`). Final whole-branch review: ready to merge, no critical/important findings (one minor forward note, not blocking: the badge pill isn't width-clipped, so a hypothetical double-digit side-seat count could still widen the pill past the 64dp column rather than truncating — worth an opportunistic spot-check, not verified in this pass). Merged to master.
- **Trick-reveal motion shipped (2026-07-10), item 7/7 — the last checklist item, Pişti is now "flawless" per the reference checklist.** The original phrase ("per-seat trick layout") comes from Batak's trick-taking mechanic and doesn't map literally onto Pişti's single shared-pile mechanic — scoped down (via an explicit AskUserQuestion on interpretation) to a motion cue instead of a real per-seat layout rebuild: the just-played card now visually travels in from the direction of whichever seat played it, landing at the same center pile as before. Brainstormed with the visual companion (a CSS `@keyframes` mockup demoing all four directions); you asked for the animation 1.7× faster than the first cut (900ms → 530ms) and asked directly whether this would be a performance risk once reused across future multiplayer games — answered concretely: it reuses the exact same native-driven `Animated.Value` `RevealCard` already used (per `[[pisti_card_ui_perf]]`), just two more interpolated outputs on the same driver, architecturally free regardless of scale. Implementation: `apps/mobile/src/games/pisti/pistiSeating.ts` gained `RevealOrigin`, a fixed `REVEAL_ORIGIN_OFFSETS` direction map (deliberately not `onLayout`-measured real seat coordinates — a simplification, not a gap), and `resolveRevealOrigin` (human → `bottom`, AI → looked up via the existing `assignSeats`); `PistiTable.tsx`'s `RevealCard` now interpolates `translateX`/`translateY` from that offset down to `0` alongside the existing opacity/scale, at 530ms with `Easing.out(Easing.cubic)`. No new component, no new tests (motion-only tweak to existing decorative UI). Visual verification (browser/Playwright) confirmed correct in-flight travel for top/left/right and surfaced a real but pre-existing, out-of-scope finding: human plays never trigger the reveal at all — see the immediately following entry, fixed the same day. Final whole-branch review: ready to merge, no critical/important findings (one minor, non-blocking note: `scale` is applied after `translate` in the transform array, so the origin displacement is measured in unscaled space, a cosmetically-negligible, pre-existing-structure quirk). Merged to master.
- **Human plays now also trigger the trick-reveal (2026-07-10), closing the gap surfaced above.** You caught in a live playtest that only the AI's played card ever traveled to the pile — the human's own play committed to state instantly with no reveal at all. Root cause: `PistiScreen.tsx`'s `handlePlayCard` called `applyMove` directly, never `setRevealedMove`; only `useAITurn`'s `onMove` went through the reveal-then-commit path (`AI_REVEAL_DELAY_MS`, renamed `REVEAL_DELAY_MS` since it was never really "AI thinking time" — that's a separate delay inside `useAITurn` — its actual purpose, pausing before commit so the travel animation has time to play, applies identically to both). Fixed by mirroring the exact same reveal-then-commit pattern for the human's own play. This surfaced two more coordinated fixes beyond the one-line rename, both in `PistiTable.tsx`: (1) the human's own hand row renders individual selectable cards (unlike opponents, which just show a face-down count), so the just-played card had to be explicitly filtered out of the hand array during its reveal window, or it would've rendered twice — once mid-flight at the pile, once still sitting in hand; (2) since engine state (and therefore `isHumanTurn`) doesn't advance until the delayed commit fires, the hand would otherwise have stayed tappable during that ~550ms window, risking a double-play from a fast second tap — added `isHumanRevealing`/`isHumanInteractive` derived state to disable the hand and drop the "your turn" gold tappable wash for that window specifically (the green turn-glow badge deliberately stays on throughout, matching how the AI's own badge already behaves during its reveal). Implemented directly on a local branch (`ui/pisti-human-reveal-animation`) rather than a sibling worktree — **per your explicit direction this session, worktrees are now opt-in only (ask first if a change seems to genuinely need one); default is a local branch named `<category>/<name>` (e.g. `ui/`, `feature/`, `game/`) directly in the main repo, so work-in-progress is visible in your normal branch list/Source Control view.** No new tests (interaction/timing wiring on existing decorative UI, same policy as the rest of this pass). Verified: typecheck clean, full suite unchanged (30/30 suites, 122/122 tests), and browser/Playwright screenshots of a real play confirm the full sequence — card lifts on first tap, travels up from the hand on the second tap with the "You played" label, lands at the pile, and only then does the hand shrink/turn pass to the AI with no leftover duplicate card. Merged to master.

**Pişti reference-checklist polish pass: complete.** All 7 items (card center art, felt texture/vignette, active-turn badge glow, card back pattern, wood-grain corners, per-seat avatars, trick-reveal motion) are shipped and merged. Two known, deliberately-deferred gaps remain open (not blocking): no native on-device visual verification has ever been done (browser/Playwright only, per `[[dev_sandbox_no_device_access]]`), and human plays don't trigger the trick-reveal animation (noted above).

**Hard AI performance risk (2026-07-07): downgraded, no longer blocking.** No adb/emulator/physical Android device is reachable from the dev sandbox, so a literal on-device profile wasn't possible. Instead, benchmarked the exact synchronous call `useAITurn` makes (`aiStrategy.chooseMove`, i.e. `minimaxChooseMove` at depth 8) directly against the compiled engine under Node: 30 full simulated hands, 720 real AI decisions — mean 0.39ms, p95 0.92ms, worst case 2.29ms. Pişti hand size caps at 4 cards even after stock redeals, so the legal-move branching factor never exceeds 4 and depth 8 covers the whole remaining hand tree; the search is small by construction, not just fast today. Node/V8 is faster than Hermes-on-Android, so this isn't a literal device number, but the margin (sub-millisecond to low-single-digit ms vs. a 16.7ms frame budget, and vs. the existing 600ms thinking-delay pause that already masks it) is wide enough that a real-device stutter is unlikely. A real-device spot-check is still worth doing opportunistically, but it no longer blocks UI work or future games. **This benchmark methodology (time the real `chooseMove` call via compiled `dist`, across many simulated hands) should be reused for other games' Hard AI once they're built**, since they'll likely share `minimaxChooseMove` and could have larger branching factors than Pişti's capped-at-4 hands.

**Next up:** Pişti's reference-checklist polish pass is complete — Klondike Solitaire is next in the game build order.

Known follow-up items deferred from Phase 1 (non-blocking, noted in the final whole-branch review):
- `RuleEngine.setup(options: unknown, ...)` loses type safety across the options-passing chain — consider a `TOptions` generic once real games multiply.
- `GameState.rngState` exists for reproducible resume but still isn't exercised by mid-game randomness: Pişti's `setup` pre-shuffles the whole deck up front, and its mid-hand redeals just deal from the already-ordered `stock` zone, consuming no further RNG. This remains open for a future game whose mid-game randomness is genuinely re-rolled (e.g. a game that reshuffles a discard pile back into a live deck).
