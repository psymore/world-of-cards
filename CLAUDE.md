@AGENTS.md

# World Cards

A cross-platform mobile platform for traditional card games (Expo/React Native/TypeScript), designed to scale to 100+ games over years. See the full product vision and constraints in the system-level project brief (also summarized in the design spec below).

## Architecture

Full architecture decisions live in:
- `docs/superpowers/specs/2026-07-06-core-architecture-design.md` — the approved design (repo structure, Card/Rule/AI engine contracts, state management, persistence, testing strategy, risks)
- `docs/superpowers/plans/2026-07-06-core-architecture.md` — the 17-task implementation plan that built it

**Repo structure:**
- `packages/engine` — pure TypeScript, zero React/React Native/Expo dependency. Card Engine, `RuleEngine`/`AIStrategy` contracts, game registry, persistence interface, statistics, and a reusable `simulateGames` test harness. Every game's rules/AI live here as pure functions over immutable, JSON-serializable state.
- `apps/mobile` — the Expo app. All RN/Expo/Reanimated/Zustand/React Navigation code lives here. Consumes `packages/engine` via an npm workspace link (`@world-cards/engine`).

**Key conventions established in Phase 1** (see the spec for full rationale):
- Engine is a pure functional core: no classes, no internal mutable state, no `Math.random()` (use the seeded `RNG` from `packages/engine/src/core/rng.ts`).
- Adding a new game = two new folders only: `packages/engine/src/games/<id>/` (rules + AI) and `apps/mobile/src/games/<id>/` (screen), registered via `registerGame`. No shared/shell code should need editing.
- Zustand: one small global store (settings) + one short-lived per-game-session store (via `createGameSessionStore`) — never one mega-store.
- Testing: Jest multi-project (`packages/engine` on Node, `apps/mobile` on jest-expo + RNTL). Every game should get simulation tests via `@world-cards/engine/testing`'s `simulateGames`, asserting card-conservation invariants over hundreds of simulated games.

**Testing policy (as of 2026-07-07, supersedes the general TDD default until revisited):** writing tests up front adds too much time to this project's pace. Default to *not* writing new tests, especially for mobile UI components/screens — ask before adding one. The exception is the engine core (Card Engine, `RuleEngine`/AI implementations, `simulateGames`-based invariant checks) — that stays test-covered by default, since correctness there is load-bearing for every game. Existing tests should still be run for regression-checking; this policy is about not writing new ones proactively.

## Card Playground (`apps/playground`)

A separate, isolated Expo app (sibling to `apps/mobile` in the same npm workspace) for prototyping card and table visual designs — border radius/color + overlay image per card group (number/face/ace), plus felt/wood-corner color for the table. Full rationale and data model: `docs/superpowers/specs/2026-07-12-card-playground-design.md`.

**Isolation is deliberate and load-bearing, not incidental:** `apps/playground` shares `packages/engine` (pure TS, zero RN dependency) and, as of 2026-07-14, `packages/ui` (`PlayingCard`, `TableFelt`, `TableWoodCorners`, `SuitIcon`, etc. — see `docs/superpowers/specs/2026-07-14-shared-ui-package-design.md`) with the rest of the repo. It never imports from `apps/mobile` directly, and `apps/mobile` never imports from or links to `apps/playground`, in development or production. Playground-only dependencies (file/image pickers, sliders) must only ever be added to `apps/playground/package.json`, never to `apps/mobile/package.json` or `packages/ui/package.json`.

**Standing rule: whenever a change is made to how cards or the table look in `apps/mobile` (or vice versa — a deliberate update to the playground's look), treat that as a trigger to explicitly discuss with the user whether/how the other side should be updated to match or extend.** Do not silently skip this conversation, and do not auto-mirror changes either — there is no automation or lint enforcing this, it is a reminder for every future session to raise it. The one exception is `packages/engine`: since `apps/playground` genuinely depends on it, breaking changes there must be checked against both `apps/mobile` and `apps/playground` as a normal correctness matter, not a discussion trigger.

## Status

**Phase 1 (core architecture): complete**, merged to `master`. Validated end-to-end against an internal test-only fixture game (`cardDraftGame`, in `packages/engine/src/rules/__fixtures__/`) — not a real game, just proof the shared contracts work. No real game exists yet.

**Phase 2 (Pişti): rules + state model + RuleEngine + AI + registration all complete**, merged to `master`. Only the mobile UI/screen remains. Per the project roadmap, games are built in this order: Pişti → Batak → Klondike Solitaire → Spider Solitaire → FreeCell → Hearts → Spades → Gin Rummy → Crazy Eights → Blackjack → Texas Hold'em. Batak was inserted ahead of Klondike at the user's explicit direction (2026-07-14) and was not on the original roadmap — it's the Turkish bidding/trump trick-taking game, closely related to the later-scheduled Spades. Each game follows: research rules → rules doc → game state design → rule engine → UI → Easy/Medium/Hard AI → tests → simulate hundreds of games → done, then move to the next game. (Pişti deliberately did AI before UI so `simulateGames`-based testing and `registerGame` — which requires `aiStrategies` — were unlocked earlier; see the AI-strategies spec below for the rationale.)

Pişti design/plan docs so far:
- `docs/superpowers/specs/2026-07-07-pisti-rules-and-state-design.md` — rules doc + `GameState`/`Move` data model
- `docs/superpowers/plans/2026-07-07-pisti-state-model.md` — implemented the `moveAllCards` core primitive + `PistiState`/`PistiMove`/`PistiSetupOptions` types
- `docs/superpowers/specs/2026-07-07-pisti-rule-engine-design.md` — `RuleEngine` file structure, `validateMove` semantics, test-coverage plan
- `docs/superpowers/plans/2026-07-07-pisti-rule-engine.md` — implemented `pistiGame: RuleEngine<PistiState, PistiMove>` in `packages/engine/src/games/pisti/rules.ts`
- `docs/superpowers/specs/2026-07-07-pisti-ai-strategies-design.md` — Easy/Medium/Hard AI approach, registration, `simulateGames` testing plan
- `docs/superpowers/plans/2026-07-07-pisti-ai-strategies.md` — implemented `pistiEasyAI`/`pistiMediumAI`/`pistiHardAI` (`packages/engine/src/games/pisti/ai/`), registered `pistiDescriptor` (new `'fishing'` `GameCategory`) via `packages/engine/src/games/pisti/index.ts`

Pişti is now registered: `getGame('pisti')` returns a full `GameDescriptor` with all three AI difficulties. `simulateGames`-based tests pass (500 easy-vs-easy games with no card-conservation violations; Hard beat Easy 154/200).

Pişti UI design/plan docs:
- `docs/superpowers/specs/2026-07-07-pisti-ui-design.md` — navigation, screen/component breakdown, state flow, testing plan
- `docs/superpowers/plans/2026-07-07-pisti-ui.md` — implemented the full playable slice: `apps/mobile/src/components/{PlayingCard,GameScreenLayout,GameResultModal}.tsx` (shared, reusable by future games), `apps/mobile/src/games/pisti/{PistiSetupView,PistiTable,PistiScreen,useAITurn}.tsx`, `apps/mobile/src/games/registry.ts`, and a new `@world-cards/engine/games/pisti` subpath export

Pişti is now fully playable end-to-end: Home → pick a difficulty → play a full hand against Easy/Medium/Hard AI → see the result → play again or return home. `useAITurn` uses a thinking-delay `setTimeout` to pace AI moves (the `InteractionManager.runAfterInteractions` wrapper it originally used was dropped during the polish pass below — RN's InteractionManager is now a deprecated no-op stub). **Frame timing has still not been profiled on a real mid-range Android device** — this remains the standing Phase 1 performance risk and should happen before Hard AI is trusted in more complex future games.

Deliberately deferred from this UI sub-project (see the design doc for rationale): Reanimated/animated transitions, save/resume via `PersistenceAdapter`, `stats:<gameId>` recording, multi-hand match play, the 4-player/2v2 variant, human-vs-human pass-and-play, and lifting `useAITurn` into a shared cross-game hook (revisit once Hearts is built). `GameResultModal`'s tests also have a known, deferred `act()` warning from the Modal's fade animation — not fixed in this pass; the correct fix if revisited is `animationType="none"` instead of `"fade"`.

Manual smoke test in Expo Go passed (2026-07-07) — the game is playable end-to-end and feels good.

**Priority change (2026-07-07):** before starting Klondike Solitaire, finish polishing Pişti to "flawless." Klondike does not start until this polish pass is done. Polish work in flight/complete:
- Stacked/stepped pile rendering in `PistiTable` (cards no longer render exactly on top of each other)
- Removed the deprecated `InteractionManager` usage from `useAITurn` (it's now a no-op stub in the installed RN version anyway)
- Expo/dependency warnings cleared via `npx expo install --check`
- Visual polish: `HomeScreen` gaming-vibe restyle, green felt background on the Pişti screen
- `packages/engine/tsconfig.json` moved off the deprecated `moduleResolution: "node"` (node10) to `"nodenext"` (+ `isolatedModules: true`), fixing the TS6-deprecation warning ahead of TS7
- Table UI overhaul (2026-07-08) fixing three usability bugs found in manual play: low-contrast text on the new green felt (pile count/banner/title recolored), the pile visibly shifting position when the hand row emptied (opponent/hand rows now have fixed-height slots so the center pile area never moves), and the opponent's move being invisible (a ~550ms "reveal" beat now shows the AI's chosen card highlighted in the pile before it's committed to state, via `revealedMove` state + a nested `setTimeout` in `PistiScreen`, not a second effect-scheduled timer — that approach broke under fake timers, see `PistiScreen.tsx`). Also added a felt "mat" under the pile and per-player captured-card-count badges.
- Second UI polish pass (2026-07-08): `PlayingCard` redesigned with corner rank/suit + a blank `centerArt` placeholder view marked for future card artwork. Hand layout split by role — local player is a plain evenly-spaced face-up row, opponent is a face-down fan (rotation + quadratic `translateY` curve, center card highest) since a flat overlap read as messy for a face-down hand. Added tap-to-select-then-tap-to-play card interaction via two new reusable pieces in `apps/mobile/src/components/`: `useCardSelection` (selection state machine) and `SelectableCard` (lift/highlight animation), intended for reuse by every future game's hand UI, not just Pişti.
- Four-player mode (2026-07-08): Pişti now supports 1 human + 3 AI, individual free-for-all (no teams), as a player-count choice on the setup screen alongside the existing 2-player game — 2-player behavior is unchanged (verified: every pre-existing rules/simulate test still passes untouched). This was a genuine N-player generalization, not a hardcoded 4p branch:
  - `packages/engine/src/games/pisti/rules.ts`: `PistiSetupOptions.players` is now `PlayerId[]` (was a 2-tuple); `setup`/redeal deal to every player generically; `calculateScore`'s majority-capture +3 bonus now goes to the sole leader across all players (no bonus on any tie, matching the original 2-player rule). Confirmed via new `rules.test.ts` cases and a 500-game 4-player `simulateGames` run with zero card-conservation violations.
  - AI strategies (`easy`/`medium`/`hard`) needed **no changes** — all three already operated generically over `state.players`, including Hard AI's minimax `evaluate` (sums all opponents' scores, not just "the" opponent). `maxDepth: 8` is unchanged for 4p, which now covers only 2 full rounds instead of 4 — not a correctness issue, just a possible future tuning point if Hard AI feels weaker at 4p.
  - `apps/mobile/src/games/pisti/useAITurn.ts`: takes `aiPlayerIds: PlayerId[]` instead of a single id, and reports which player moved (`onMove(move, playerId)`) so the reveal-beat logic in `PistiScreen` works for any AI seat, not just one.
  - `apps/mobile/src/games/pisti/PistiTable.tsx`: seats are now data-driven (`assignSeats`) — 1 opponent sits at the top (unchanged 2p look), 3 sit left/top/right. Side seats show a vertical card stack rather than a rotated horizontal fan — RN transforms don't affect layout sizing, so a 90°-rotated fan would've kept its wide unrotated footprint reserved in the row, risking overflow on a phone-width screen; deliberately chose the safer option since this can't be visually verified in this sandbox.
  - Setup screen (`PistiSetupView`) gained a 2/4-player picker above the difficulty picker; all 3 AI opponents share one difficulty (no per-seat difficulty), per your call.
- Counter-clockwise turn order + partner (2v2) mode (2026-07-08):
  - **Counter-clockwise fix**: seating was accidentally clockwise (bottom→left→top→right). `PistiTable.assignSeats` now maps turn order to bottom→right→top→left, matching `state.players` order (human, then AI seats in turn order).
  - **Partner mode**: added as a second axis alongside player count — the 4-player setup screen now also asks "Free-for-all" vs "With a Partner". Engine-level: `PistiSetupOptions`/`PistiState` gained an optional `teams: PlayerId[][] | null` field; `calculateScore` was refactored around a "scoring unit" concept (a team's pooled members, or a lone player in free-for-all) so both modes share one code path — free-for-all is `units = players.map(p => [p])`, which reduces to the exact old per-player math (verified: all pre-existing tests pass untouched). Partner is always whichever AI seat sits across the table (top seat, second in turn order) — geometrically unambiguous since there's only one human. Verified with a 500-game team-mode simulation asserting teammates always finish with equal pooled scores.
  - **Caught a real correctness trap**: `GameResultModal` computed "is this a tie" from `winners.length > 1`. In team mode a clean team win naturally produces 2 winners (both teammates share the pooled score), which would have wrongly shown "It's a tie!" instead of "You win!". Fixed by counting *distinct teams* among the winners instead of raw winner count (`teams` is an optional prop, so free-for-all behavior — where every winner is its own "team" — is unchanged).
  - AI strategies again needed no changes — teams only affect scoring, not legal moves or card play.
- **Not yet visually verified on a native device** — the 2-seat polish, 4-seat layout, and counter-clockwise/partner seating from the bullets above predate the browser-verification workaround below and still haven't been screenshotted; needs a manual check in Expo Go or the browser before considering this "flawless." (Reminder for future sessions: the user said they'll set up adb/emulator/browser access later — keep flagging the *native* gap even once browser checks are routine.)
- **Browser-based visual verification unlocked (2026-07-08):** installed `react-native-web`/`react-dom` so `expo start --web` runs, plus `playwright-core` driving the sandbox's already-installed system Chrome (`executablePath`, no bundled browser download needed) to take real screenshots. Not a substitute for a native device pass, but a real step up from zero visual verification — see `[[dev_sandbox_no_device_access]]` memory. First use of this workflow immediately caught a real bug: `PistiSetupView` (the table-size/difficulty picker) was completely unstyled (default RN grey), and — more seriously — had no way back to Home at all (`Game` stack has `headerShown: false` and the view wasn't wrapped in `GameScreenLayout`). Fixed: themed to the app's navy/gold tokens and given a working `onBack` header link; `PistiSetupView.test.tsx` updated (not expanded) to pass the new required prop. Lesson written to memory: this changelog records *intent*, not *verified rendered state* — don't trust "polish pass" narrative entries as visual proof without an explicit screenshot mention.
- **Alper Games (Batak HD / Spades Batak) adopted as the explicit visual reference** for the Pişti table (2026-07-08), at the user's direction — three reference screenshots checked into `docs/references/pisti/` (see its README for sources/attribution). Published as a Claude Artifact checklist ranking the gaps cheapest→biggest lift: card center art, felt texture/vignette, active-turn glow on the player badge, card back pattern, wood-grain table frame, per-seat avatars, per-seat trick layout (played cards near the player who played them, not merged into one pile — biggest lift, do last). Explicitly out of scope: coin/stake badges, chat/emote, and the suit-selector bidding UI — all tied to Batak features (wagering, human social play, trump bidding) Pişti doesn't have.
- **Card center art shipped (2026-07-08), first item off that checklist.** Rejected both a literal port of the Batak reference art (illustrated court-card portraits, traditional pip layouts) and a "just enlarge the Unicode glyph" patch — at this component's actual render size (56×80 normal / 36×52 small), pip layouts read as cluttered noise and illustrated portraits would blur, while Unicode suit glyphs (`♥♦♣♠`) render inconsistently across platforms/fonts. Instead added `apps/mobile/src/components/SuitIcon.tsx` — hand-authored SVG paths (via new `react-native-svg` dependency, installed through `npx expo install`) for all four suits, reused for **both** the corner index and a new large, low-opacity (`0.16`) centered "watermark" suit shape in `PlayingCard.tsx`'s previously-empty `centerArt` placeholder. Same treatment for every rank (no per-rank pip counting) — deliberately deferred illustrated court-card art as a separate, bigger future investment (real art sourcing/curation, not a quick patch). Considered and declined AI-image-generated art for this pass: stylistic consistency across a full suit/rank set requires a curation pipeline (transparent-background cleanup, palette matching) that isn't worth it for four simple geometric shapes; flagged as worth reconsidering specifically for a future illustrated-court-card project. `PlayingCard.test.tsx` updated (not expanded) to query the new `corner-suit-<suit>` testID instead of the old Unicode text. Visually verified via the browser workflow above — screenshots confirm all four suit icons render crisp and legible at both corner and watermark size, no console errors.
- **Felt texture/vignette and active-turn badge glow shipped (2026-07-09), items 2–3 off the checklist.** Both scoped entirely to `apps/mobile/src/games/pisti/PistiTable.tsx` (no shared-component changes). Felt texture: a static `FeltTexture` overlay (`react-native-svg` `Pattern` for a faint diagonal diamond weave + `RadialGradient` for edge-darkening vignette), absolutely positioned behind all table content, `pointerEvents="none"`, painted once and never re-rendered by game state — zero runtime cost during play. Active-turn indicator: replaced relying solely on the existing whole-area gold wash (`activeArea`, kept as-is for the "these cards are tappable" affordance) with a dedicated pill-shaped `badge` wrapping each player's name label; `badgeActive` adds a bright green border + shadow-based glow (`#4ade80`) matching the third reference screenshot's "glow ring around the whole badge," clearly distinguishable from the general gold tint. Visually verified: felt weave and vignette are visible but subtle, the active player's badge glows green while the inactive one stays a plain dark pill. Full mobile test suite (38 tests) still passes untouched — no test asserted on the unwrapped label markup.
- **Card back pattern shipped (2026-07-09), item 4 off the checklist.** Scoped to `apps/mobile/src/components/PlayingCard.tsx` (the shared card component, not table-specific). Deliberately did not copy the reference's red rosette literally — recolored to the app's own navy/gold identity (`back` bg changed from a generic mid-blue `#2f5fa8` to a deep navy `#1c2451`) instead of importing Alper Games' brand color. New `CardBackPattern` (`react-native-svg`) layers a faint gold diagonal-diamond lattice, a thin inset gold frame, and a simple centered gold lozenge emblem — same "keep it simple at 36–56px" reasoning as the center-art decision: an ornate rosette would blur at this render size, so the emblem is a plain geometric shape, not per-suit/per-rank detail. `back` style gained `overflow: 'hidden'` so the pattern clips to the card's rounded corners. No test changes needed (no test asserted on back-face internals beyond the existing `playing-card-back` testID). Visually verified via a close-up crop of the opponent's fanned hand — frame, lattice, and emblem all read cleanly at actual card size, no console errors.
- **Performance pushback + `React.memo` pass (2026-07-09).** You raised a valid concern: `PlayingCard`/`PistiTable` are shared across every future game, so the SVG additions above need to stay light. Assessment: architecturally cheap (native vector draws, no images/network, ~25-30 small SVG instances even on a 4-player table, only redrawn on discrete move events since the reveal/capture animations already run on the native thread via `useNativeDriver: true`) — but this is reasoning, not a measurement, since there's still no on-device profiling capability in this sandbox. Concretely, not just reassurance: wrapped `SuitIcon`, `CardBackPattern`, `PlayingCard` itself, and `FeltTexture` in `React.memo` (nothing in the app was memoized before this). Confirmed the engine reuses `Card` object references across zone-array rebuilds (`packages/engine/src/core/table.ts`'s `[...cards]` spreads don't clone individual cards), so `PlayingCard`'s memo on the `card` prop is a real win for the ~20+ cards that didn't move on a given turn, not a no-op. See `[[pisti_card_ui_perf]]` memory for the full reasoning, including the one caveat (some `PistiTable` call sites pass a freshly-constructed `style` object each render, which defeats the outer `PlayingCard` memo specifically for those — left as-is since the inner `SuitIcon`/`CardBackPattern` memos still catch the real cost). Full mobile suite (38 tests) + typecheck both clean after the change; re-screenshotted to confirm zero visual regression.
- **`pointerEvents` prop deprecation fixed (2026-07-09).** You reported console warnings from clicking around the web preview. Triaged: three of the four were `react-native-web`-only noise (`textShadow*`/`shadow*` prop deprecation — the *correct* form on native, since real RN has no `boxShadow`; `useNativeDriver`/`RCTAnimation missing` — `react-native-web` has no native animation driver at all, the "run `pod install`" advice in that message is native-iOS boilerplate that doesn't apply here; and the `Web Bundled`/`Running application` lines, which are just normal Metro dev-server logs) — left alone since web isn't a real target for this app, just the sandbox's screenshot tool. The fourth (`props.pointerEvents is deprecated. Use style.pointerEvents`) was real and ours: `FeltTexture`/`CardBackPattern` used the `pointerEvents="none"` prop form instead of `style.pointerEvents`. Fixed in both; confirmed via a console-log capture that our code no longer emits it (one instance still fires from a third-party dependency, before Pişti even loads — not ours to fix).
- **`/simplify` cleanup pass (2026-07-09)**, prompted by you flagging that `PlayingCard`/`PistiTable` need to stay modular and DRY since every future game depends on them. Ran 4 parallel review agents (reuse/simplification/efficiency/altitude) over the uncommitted diff; findings converged strongly enough to act on:
  - **Killed real duplication**: `PistiTable.tsx`'s top/left/right opponent-seat blocks were the same ~15-line filter+map+derivation (handCount/isCurrentTurn/isRevealing) copy-pasted three times — `OpponentSeat` now takes `state`/`revealCard` directly and derives all of that internally once; call sites collapsed to a 3-line `OpponentSeatGroup` helper per layout slot. Also pulled the badge/label JSX (duplicated between opponents and the human row) into a local `PlayerBadge`.
  - **New shared components** (`apps/mobile/src/components/`): `AbsoluteOverlay.tsx` (the pointer-events-none absolute-fill wrapper `FeltTexture` and `CardBackPattern` had each hand-rolled separately, including a duplicated `noPointerEvents` style literal), `glowShadow.ts` (the "colored glow" shadow recipe `PlayingCard`'s `highlighted` style and `PistiTable`'s `badgeActive` style had each hand-rolled with different radii), `TableFelt.tsx` (promoted from `PistiTable`'s local `FeltTexture` — it was already zero-prop and Pişti-agnostic, and felt tables are near-universal for the games still on the roadmap, so extracting now beat waiting for a second consumer to copy-paste it), `CardBackPattern.tsx` (moved out of `PlayingCard.tsx` into its own file, matching the existing `SuitIcon.tsx` precedent).
  - **New `apps/mobile/src/games/pisti/pistiSeating.ts`**: pulled the pure, side-effect-free geometry (`assignSeats`, `fanRotationDeg`, `fanCurveY`, `SeatPosition`, the fan/stack overlap constants) out of `PistiTable.tsx` — no JSX/RN dependency, independently unit-testable, and it shrank the table file materially (**376 → 289 lines**; `PlayingCard.tsx` went **148 → 96** after the `CardBackPattern` extraction). Also exports a new `PARTNER_SEAT_INDEX` constant: `PistiScreen.tsx`'s `buildTeams`/`buildPlayerNames` used to hardcode `aiIds[1]` as "the partner" based on nothing but a *comment* pointing at `PistiTable.assignSeats` in a different file — a real "two places encode the same invariant with no shared source of truth" bug risk the altitude-review agent caught. Both now derive from the one constant.
  - **`PILE_CARD_OFFSETS`-style fix for `React.memo` effectiveness**: the side-seat card stack was passing a freshly-allocated `{marginTop: ...}` style object on every render, silently defeating `PlayingCard`'s memo from the previous entry — replaced with a precomputed `SIDE_CARD_STYLES` lookup array (same pattern already used for the pile stack) so the same object reference is reused across renders.
  - `rules.ts`'s `calculateScore` majority-bonus lookup simplified from a `reduce`+spread building an unused array of leader indices down to a plain `filter().length` + `indexOf()`. `PistiScreen.tsx`'s `buildPlayerNames` (session-constant inputs) wrapped in `useMemo` so it's not rebuilt every move.
  - **Explicitly skipped** (per the review agents' own judgment, not just mine): further splitting `PistiTable.tsx`'s `RevealCard`/`OpponentSeat` into separate files (each is small, used once, tightly coupled to this table's styles — splitting would trade "one file, clear sections" for "five files to jump between"); extracting the navy/gold color literals duplicated between `HomeScreen.tsx` and `PistiSetupView.tsx` into a shared theme module (real, but a bigger/riskier foundational decision than a cleanup pass should smuggle in — flagged for a future deliberate pass); precomputing the top-seat fan transform the same way as the side stack (depends on an (index, count) pair, low element count, not worth the added complexity given it's already a known, accepted tradeoff per `[[pisti_card_ui_perf]]`).
  - Verified: typecheck clean on both packages, full test suite **122/122 passing across 30 suites** (mobile + engine, including the 500-game team-mode simulation), and re-screenshotted both the 2-player table and — since `buildTeams` was touched — the 4-player "with a partner" table specifically, confirming Partner/Opponent 1/Opponent 2 seating is pixel-identical to before the refactor.
- **Wood-grain corner accents shipped (2026-07-10), item 5 off the checklist.** Brainstormed with the visual companion first: three frame styles were mocked (full picture-frame border, top/bottom rails only, corner wedges only) — you picked corner wedges (C), with badges staying separate/floating rather than nested into the wedge shape, and dark mahogany over amber oak for the wood tone. The other two styles are documented as deferred alternatives, not built as an unused variant system — mockup HTML persists under `.superpowers/brainstorm/` (gitignored) and the choice/rationale is recorded in `docs/superpowers/specs/2026-07-10-pisti-wood-corners-design.md`. New `apps/mobile/src/components/TableWoodCorners.tsx`: four quarter-circle wood wedges (react-native-svg gradient + faint grain pattern + thin gold `#ffd966` trim stroke) pinned at fixed 56dp size to the table container's four corners, following the exact `TableFelt`/`CardBackPattern` convention (zero-prop, `AbsoluteOverlay`-wrapped, `React.memo`'d) — rendered in `PistiTable.tsx` right after `<TableFelt />`, so same z-order, no seat-count dependency (identical for 2p/4p). Built via subagent-driven-development in an isolated sibling worktree (per `[[worktree_jest_nesting_bug]]`): implementer transcribed the plan's exact code, task review confirmed the SVG arc geometry is genuinely convex (not an accidentally-concave notch) across all four corners, and visual verification via the browser/Playwright workflow (`[[dev_sandbox_no_device_access]]`) screenshotted both the 2-player and 4-player tables — wedges render correctly, no overlap with the 4-player side-seat card stacks, no new console warnings. No new tests (pure decorative UI, per policy). Final whole-branch review: ready to merge, no critical/important findings (two minor, non-blocking notes: the grain texture is barely visible at this render size, and the wood/gold colors are hardcoded constants like `TableFelt`'s — both swept into the already-tracked future navy/gold theme-extraction pass rather than fixed here). Merged to master.
- **Per-seat player avatars shipped (2026-07-10), item 6 off the checklist.** Brainstormed with the visual companion again: glyph style (generic silhouette vs. a `SuitIcon`-monogram vs. an initial-letter monogram — silhouette chosen, literal match to the reference) and placement (avatar stacked above the badge like the reference vs. merged inline into it — inline chosen, since a stacked layout doesn't fit the 4-player mode's 64dp side-seat width budget). New `apps/mobile/src/components/PlayerAvatar.tsx`: small SVG person-silhouette icon in a circular ring, `React.memo`'d, following the `SuitIcon` convention — one prop, `accent?: boolean` (deliberately generic, not `isHuman`; the caller decides). Gold ring = a static "this is you" marker on the human seat only; every AI seat (however many, 1 or 3) renders an identical plain-ringed avatar — this is independent of the pre-existing green `badgeActive` active-turn glow, and both signals are confirmed simultaneously legible in screenshots. `PlayerBadge` in `PistiTable.tsx` now renders the avatar inline before the name/capture-count text, inside the same pill. Visual verification (browser/Playwright workflow) caught a real regression mid-task: the avatar broke the 4-player mode's 64dp-wide side seats, wrapping badge text onto 5 broken lines — root-caused to the avatar+gap+padding leaving too little width for text in that column. Fixed with a `size?: 'normal' | 'small'` prop on `PlayerAvatar` and a `compact` prop on `PlayerBadge`, applied only to the two side seats (top/human badges are pixel-identical to before the fix). **This is a deliberate, surfaced deviation from the original plan's "identical avatar size across every seat type" constraint** — shown to you as a concrete before/after tradeoff (screenshots of the broken wrap vs. the fix), you chose to keep the per-seat sizing over forcing one size everywhere. A follow-up task review caught a real Important gap in that fix (no text-truncation safeguard, only verified at capture counts 0 and 5, not the double-digit counts a real hand can produce) — fixed with `numberOfLines={1}` on the badge label, re-reviewed clean. No new tests (pure decorative/presentational branching, same policy as `CardBackPattern`). Final whole-branch review: ready to merge, no critical/important findings (one minor forward note, not blocking: the badge pill isn't width-clipped, so a hypothetical double-digit side-seat count could still widen the pill past the 64dp column rather than truncating — worth an opportunistic spot-check, not verified in this pass). Merged to master.
- **Trick-reveal motion shipped (2026-07-10), item 7/7 — the last checklist item, Pişti is now "flawless" per the reference checklist.** The original phrase ("per-seat trick layout") comes from Batak's trick-taking mechanic and doesn't map literally onto Pişti's single shared-pile mechanic — scoped down (via an explicit AskUserQuestion on interpretation) to a motion cue instead of a real per-seat layout rebuild: the just-played card now visually travels in from the direction of whichever seat played it, landing at the same center pile as before. Brainstormed with the visual companion (a CSS `@keyframes` mockup demoing all four directions); you asked for the animation 1.7× faster than the first cut (900ms → 530ms) and asked directly whether this would be a performance risk once reused across future multiplayer games — answered concretely: it reuses the exact same native-driven `Animated.Value` `RevealCard` already used (per `[[pisti_card_ui_perf]]`), just two more interpolated outputs on the same driver, architecturally free regardless of scale. Implementation: `apps/mobile/src/games/pisti/pistiSeating.ts` gained `RevealOrigin`, a fixed `REVEAL_ORIGIN_OFFSETS` direction map (deliberately not `onLayout`-measured real seat coordinates — a simplification, not a gap), and `resolveRevealOrigin` (human → `bottom`, AI → looked up via the existing `assignSeats`); `PistiTable.tsx`'s `RevealCard` now interpolates `translateX`/`translateY` from that offset down to `0` alongside the existing opacity/scale, at 530ms with `Easing.out(Easing.cubic)`. No new component, no new tests (motion-only tweak to existing decorative UI). Visual verification (browser/Playwright) confirmed correct in-flight travel for top/left/right and surfaced a real but pre-existing, out-of-scope finding: human plays never trigger the reveal at all — see the immediately following entry, fixed the same day. Final whole-branch review: ready to merge, no critical/important findings (one minor, non-blocking note: `scale` is applied after `translate` in the transform array, so the origin displacement is measured in unscaled space, a cosmetically-negligible, pre-existing-structure quirk). Merged to master.
- **Human plays now also trigger the trick-reveal (2026-07-10), closing the gap surfaced above.** You caught in a live playtest that only the AI's played card ever traveled to the pile — the human's own play committed to state instantly with no reveal at all. Root cause: `PistiScreen.tsx`'s `handlePlayCard` called `applyMove` directly, never `setRevealedMove`; only `useAITurn`'s `onMove` went through the reveal-then-commit path (`AI_REVEAL_DELAY_MS`, renamed `REVEAL_DELAY_MS` since it was never really "AI thinking time" — that's a separate delay inside `useAITurn` — its actual purpose, pausing before commit so the travel animation has time to play, applies identically to both). Fixed by mirroring the exact same reveal-then-commit pattern for the human's own play. This surfaced two more coordinated fixes beyond the one-line rename, both in `PistiTable.tsx`: (1) the human's own hand row renders individual selectable cards (unlike opponents, which just show a face-down count), so the just-played card had to be explicitly filtered out of the hand array during its reveal window, or it would've rendered twice — once mid-flight at the pile, once still sitting in hand; (2) since engine state (and therefore `isHumanTurn`) doesn't advance until the delayed commit fires, the hand would otherwise have stayed tappable during that ~550ms window, risking a double-play from a fast second tap — added `isHumanRevealing`/`isHumanInteractive` derived state to disable the hand and drop the "your turn" gold tappable wash for that window specifically (the green turn-glow badge deliberately stays on throughout, matching how the AI's own badge already behaves during its reveal). Implemented directly on a local branch (`ui/pisti-human-reveal-animation`) rather than a sibling worktree — **per your explicit direction this session, worktrees are now opt-in only (ask first if a change seems to genuinely need one); default is a local branch named `<category>/<name>` (e.g. `ui/`, `feature/`, `game/`) directly in the main repo, so work-in-progress is visible in your normal branch list/Source Control view.** No new tests (interaction/timing wiring on existing decorative UI, same policy as the rest of this pass). Verified: typecheck clean, full suite unchanged (30/30 suites, 122/122 tests), and browser/Playwright screenshots of a real play confirm the full sequence — card lifts on first tap, travels up from the hand on the second tap with the "You played" label, lands at the pile, and only then does the hand shrink/turn pass to the AI with no leftover duplicate card. Merged to master.

**Pişti reference-checklist polish pass: complete.** All 7 items (card center art, felt texture/vignette, active-turn badge glow, card back pattern, wood-grain corners, per-seat avatars, trick-reveal motion) are shipped and merged. Two known, deliberately-deferred gaps remain open (not blocking): no native on-device visual verification has ever been done (browser/Playwright only, per `[[dev_sandbox_no_device_access]]`), and human plays don't trigger the trick-reveal animation (noted above).

**Hard AI performance risk (2026-07-07): downgraded, no longer blocking.** No adb/emulator/physical Android device is reachable from the dev sandbox, so a literal on-device profile wasn't possible. Instead, benchmarked the exact synchronous call `useAITurn` makes (`aiStrategy.chooseMove`, i.e. `minimaxChooseMove` at depth 8) directly against the compiled engine under Node: 30 full simulated hands, 720 real AI decisions — mean 0.39ms, p95 0.92ms, worst case 2.29ms. Pişti hand size caps at 4 cards even after stock redeals, so the legal-move branching factor never exceeds 4 and depth 8 covers the whole remaining hand tree; the search is small by construction, not just fast today. Node/V8 is faster than Hermes-on-Android, so this isn't a literal device number, but the margin (sub-millisecond to low-single-digit ms vs. a 16.7ms frame budget, and vs. the existing 600ms thinking-delay pause that already masks it) is wide enough that a real-device stutter is unlikely. A real-device spot-check is still worth doing opportunistically, but it no longer blocks UI work or future games. **This benchmark methodology (time the real `chooseMove` call via compiled `dist`, across many simulated hands) should be reused for other games' Hard AI once they're built**, since they'll likely share `minimaxChooseMove` and could have larger branching factors than Pişti's capped-at-4 hands.

**Phase 3 (Batak, 4-player individual): rules/state design, rule engine, AI strategies, and the mobile UI are all complete and merged to `master`. Batak is fully playable end-to-end.** Inserted ahead of Klondike per the user's explicit direction — see the roadmap note above. Scoped to 4-player individual play only for this first sub-project; the 3-player "gömmeli" (buried-kitty) variant and the 4-player "eşli" (partnered) variant the user ultimately wants are deliberately deferred to their own follow-up sub-projects once this vertical slice (rules → engine → AI → UI → tests) is fully working end-to-end, mirroring how Pişti itself grew incrementally (2p → 4p free-for-all → partner mode).

Batak design/plan docs so far:
- `docs/superpowers/specs/2026-07-14-batak-rules-and-state-design.md` — rules doc (bidding/trump/trick-play/scoring, single-hand scope) + `BatakState`/`BatakMove`/`BatakSetupOptions` data model. Ruleset was researched from public sources (GameRules.com, pagat.com, Çiftokey) but the final rules — bid minimums, the forced-contract-on-all-pass value (4, not the normal 5 floor), the no-×10 scoring formula, and the universal zero-tricks bust penalty — were corrected and confirmed directly by the user, which is the authoritative source where it differs from the initial web research. Multi-hand match play, the cumulative target score, and the bid-13 ("Draw") instant-match-win rule are explicitly deferred to a future match-layer sub-project.
- `docs/superpowers/plans/2026-07-14-batak-state-model.md` — implemented `packages/engine/src/games/batak/ranking.ts` (ace-high rank comparator) and `packages/engine/src/games/batak/types.ts` (`BatakState`/`BatakMove`/`BatakSetupOptions`). Caught and fixed a real spec bug during execution: `packages/engine/src/core/ranking.ts`'s pre-existing `createRankComparator` (a Phase 1 primitive, unused until now) is low-to-high by convention, but the spec's rank order was written ace-first/high-to-low — the plan's literal prescribed code would have failed its own tests. Fixed with a sign-negation wrapper (`compareRanks`), independently re-verified by both the task reviewer and the final whole-branch reviewer against `core/ranking.ts`'s actual `ai - bi` semantics, now documented inline with a comment. Also corrected the spec itself: it originally called for a new shared rank-comparison primitive, but `core/ranking.ts` already had one — no new core primitive was needed. Final whole-branch review: ready to merge, no critical/important findings (one minor, deferred note: the `currentTrick` state field and the `trick` zone hold overlapping data by design — mirroring Pişti's `lastCapturedBy` pattern — the future RuleEngine implementer will need to cross-reference both to resolve suit/rank for the mandatory-raise and trick-winner logic). Merged to `master`.

- `docs/superpowers/specs/2026-07-14-batak-rule-engine-design.md` — file structure (`packages/engine/src/games/batak/rules.ts` + `rules.test.ts`), `validateMove` semantics (unlike Pişti's independent one-line check, Batak's `'play'` case reuses `getLegalMoves` as the single source of truth, since legality — mandatory raise, trump-broken-to-lead — is genuinely non-trivial), and the test-coverage plan.
- `docs/superpowers/plans/2026-07-14-batak-rule-engine.md` — implemented `batakGame: RuleEngine<BatakState, BatakMove>` across 3 tasks (scaffold; the bidding auction + trump-selection state machine; trick-taking logic). Caught and fixed a real plan bug during self-review, before any task was even dispatched: the auction-closing condition (`activePlayers.length === 1`) would have crowned a player who never got a turn as bid winner with a contract of 0, whenever everyone before them in seating order passed without anyone ever bidding — fixed by only closing early once the sole remaining active player has actually placed a bid, otherwise they get their own turn first. **A distinct pattern showed up three separate times across this plan's review cycles** (2 task-level, 1 final): game logic that was correct, paired with a test that couldn't actually distinguish it from a plausible bug, because every test exercising that code path used a single-element collection (one trick, one trump, one led-suit card) where "highest" and "first"/"last" happen to coincide. All three — the auction-closing guard, `trickWinnerIndex`'s multi-trump resolution, and `playingLegalMoves`' mandatory-raise threshold — were fixed the same way: add a multi-element test where "correct" and "plausible-but-wrong" diverge, then prove it with actual mutation testing (temporarily break the code, confirm the new test fails, revert, confirm it passes) rather than just asserting the fix is right. Worth watching for as a recurring blind spot in any future game with similar "find the extreme element" logic (trick-taking games generally). Final whole-branch review: ready to merge after the 1 fix cycle above, no other critical/important findings (minor, deferred: the `compareRanks`-based "find highest card" reduce is duplicated 3× in one file — not worth extracting at this scale per two independent reviewers). Merged to `master`.

- `docs/superpowers/specs/2026-07-14-batak-ai-strategies-design.md` — Easy/Medium/Hard `AIStrategy<BatakState, BatakMove>` design, registration, and `simulateGames` testing plan. Structurally different from Pişti's AI sub-project: bidding/trump-selection/card-play are three distinct decision phases with no useful immediate-score signal to greedily chase (unlike Pişti, where every play could capture), so a full-hand minimax from the bidding phase is infeasible. A shared `handStrength.ts` heuristic (bid/trump judgment identical for Medium and Hard) was designed through several rounds of user-directed correction: the user caught a wrong first-draft heuristic that would have picked a short-strong Hearts suit over a long-weak Spades suit for trump, prompting web research confirming trump SELECTION should be driven by suit length alone (honors only break length ties) — a real domain-knowledge gap closed via research, not assumption. Non-trump honor points are discounted (A=2/K=1/Q=0/J=0 vs. trump's full A=4/K=3/Q=2/J=1) per explicit user choice. User explicitly acknowledged this heuristic is a v1 approximation that will likely need rework once the 3-player/eşli variants are built (partnered hand evaluation genuinely differs), and approved proceeding anyway.
- `docs/superpowers/plans/2026-07-14-batak-ai-strategies.md` — implemented `batakEasyAI`/`batakMediumAI`/`batakHardAI` (`packages/engine/src/games/batak/ai/`) across 6 tasks (shared heuristic; Easy; `trickWinnerIndex` export + Medium; Hard; registration; simulation tests), and registered `batakDescriptor` (`category: 'trick-taking'`, no registry change needed) via `packages/engine/src/games/batak/index.ts`. Medium's card play is "win cheaply via `trickWinnerIndex` simulation, else dump low"; Hard's is `minimaxChooseMove` at `maxDepth: 4` (current-trick-only lookahead, since Batak's card-play branching factor is far larger than Pişti's capped-at-4 hands) with the same "everyone else is my adversary" `evaluate` as Pişti's 4-player Hard AI. The plan document itself was accidentally lost from the working tree mid-session (written but never committed before task dispatch began) — recovered from conversation context and committed with the user's confirmation before continuing. Every task's review independently re-verified the recurring "extreme-element tested with a single candidate" bug class from the RuleEngine sub-project did not recur here: both new `reduce`-based searches (Medium's cheapest-winning-card pick, `chooseTrumpSuit`'s length/honor comparison) have genuinely discriminating multi-candidate tests. The Hard AI test suite includes a fully hand-traced minimax scenario proving Hard's depth-4 search correctly leads a card that wins the trick outright where Medium's one-ply "always lead lowest" heuristic loses it — traced against the real engine code during planning and confirmed to match the implementer's actual test run exactly. `simulateGames` confirms 500 four-player easy-vs-easy-vs-easy-vs-easy games run with no card-conservation violations, and Hard AI won 88/200 (44%) against three Easy AI players, well clear of the 25% chance baseline and the 35% test threshold. Final whole-branch review: ready to merge, no critical/important findings (minor, deferred notes: mid-hand `calculateScore` evaluation is dominated by bust penalties early in a hand and the Hard-vs-Easy win-rate test can't isolate card-play quality from bidding quality; worst-case depth-4 branching can exceed "current trick only" when a follower is void early in a hand — both folded into the already-tracked on-device-profiling/recalibration follow-up). Merged to `master`.

Batak is now registered: `getGame('batak')` returns a full `GameDescriptor` with all three AI difficulties, mirroring Pişti's shape.

- `docs/superpowers/specs/2026-07-15-batak-ui-design.md` — mobile UI design (fourth Batak sub-project). One deliberate structural divergence from Pişti's own UI design, confirmed with the user rather than assumed: per-seat trick layout (each of the 4 played cards renders near the seat that played it) built as the real first design from day one, not deferred as "future polish" the way Pişti's UI originally deferred it — Batak had no existing shared-pile mechanic to retrofit, so there was no extra cost to doing it right immediately. Bidding and trump-selection are integrated directly onto the same table component as trick play, no separate screen. No animated card-travel motion this pass, except a non-animated ~1.1s pause holding the trick-completing 4th card visible before it commits — required because `batakGame.performMove` resolves a completed trick atomically (winner computed and cards swept within one call), so without staging, the UI would show the 4th card appear and the whole trick vanish in the same instant. No new UI tests, per the standing 2026-07-07 testing policy (the first mobile UI sub-project since that policy took effect).
- `docs/superpowers/plans/2026-07-15-batak-ui.md` — implemented `BatakSetupView`/`BatakTable`/`BatakScreen` (`apps/mobile/src/games/batak/`) across 7 tasks, registered via `apps/mobile/src/games/registry.ts` and a new `@world-cards/engine/games/batak` subpath export (mirroring Pişti's own one-time addition). Two pieces of existing Pişti-only code were extracted into shared, game-agnostic locations as part of this plan — the seat-assignment/fan-geometry math (`apps/mobile/src/table/seating.ts`) and the `useAITurn` hook (`apps/mobile/src/hooks/useAITurn.ts`) — since Batak is genuinely their second real use (both had doc comments anticipating exactly this extraction trigger). A real cross-task risk was caught and fixed during planning itself, before any task was dispatched: the seating module's `MAX_SIDE_STACK_CARDS` constant had to be bumped from Pişti's original `8` to `13`, since Batak hands start at 13 cards (Pişti's never exceeded 4) — a literal copy-paste would have silently produced `undefined` styles for the 9th–13th stacked opponent cards. Every task's reviewer independently verified this fix landed correctly, and it was also confirmed live: a full 13-card face-down stack rendered cleanly in manual visual verification. All 6 automated tasks passed review clean with zero fix cycles.
  - **Task 7 (manual visual verification, done directly rather than via subagent, since it requires visual judgment):** used the existing browser/Playwright workflow (`[[dev_sandbox_no_device_access]]`) to screenshot and interact with the running app. Confirmed: Home lists Batak; the setup screen works; the bidding UI (bid-button row, per-seat bid/pass badges) renders correctly; both trump-selection branches work (the human-wins-bid 4-suit picker, screenshotted directly, and the AI-wins-bid auto-resolve path); per-seat trick-slot positioning is pixel-correct (verified via actual DOM `boundingBox()` measurements, not just eyeballing — this caught and *disproved* an initial false alarm where a played card looked visually offset but was measured to be exactly aligned); mandatory suit-following legal-move filtering works correctly in the live browser (verified via computed `opacity` across turns — all cards dim to 0.5 off-turn, a genuine legal/illegal mix appears on-turn); multi-trick progression (`tricksWon` counts, hand-shrinking, active-turn glow) was confirmed correct across a real 10-trick run; and a complete 13-trick hand was played through via automated script to the result modal, which showed correct scoring (a bidder who missed a high contract scored the negative contract value, exactly per the rules) and working Play Again/Back to Home buttons. Zero console errors throughout every run — only the same pre-existing, already-documented `react-native-web`-only noise. No real bugs found; the one thing flagged mid-verification (a seemingly-offset trick card) was investigated with DOM measurements and confirmed to be correct per-seat positioning, not a bug — a useful reminder to verify with real measurements before "fixing" something that only looks wrong in a screenshot.
  - Did not directly screenshot the ~1.1s trick-completion pause's exact mid-frame (the timing window is brief and hard to catch reliably via scripted automation), but its logic was independently traced against `rules.ts` in the task-level review and confirmed exactly correct (not off-by-one), and dozens of tricks completed correctly across the manual playthroughs without issue.
  - **Not yet visually verified on a native device** — same standing gap as Pişti, browser/Playwright only.

Batak is now fully playable end-to-end: Home → pick a difficulty → bid → (maybe) choose trump → play all 13 tricks against 3 AI opponents → see the result → play again or return home.

**Phase 3 sub-project (3-player "gömmeli" rule engine): complete, merged to `master` (2026-07-15).** First of the two deferred variants (see the Phase 3 status line above) — the buried-kitty variant, not the eşli/partnered one. Scoped to the rule engine only; AI-strategy adjustments and UI are separate follow-up sub-projects, not yet started.

- `docs/superpowers/specs/2026-07-15-batak-gomeli-rules-and-state-design.md` — rules doc, generalized entirely from `players.length` via one derived-constants table rather than a mode flag: 16-card hands (48÷3, 4-card kitty), bid floor 8 (was 5), max bid 16 (was 13), forced-all-pass contract 7 (was 4), bust threshold <2 tricks (was <1). Trump is chosen *before* the kitty exchange; the kitty's 4 cards are revealed to all players when picked up, the bidder's 4-card discard back stays hidden — a UI-layer distinction, not an engine one (state stays fully omniscient, same as opponent hands today).
- `docs/superpowers/specs/2026-07-15-batak-gomeli-rule-engine-design.md` — resolved the one open implementation question: `getLegalMoves` for the new `'kitty-exchange'` phase fully enumerates all C(20,4)=4845 possible bury combinations (matching how `'play'` legality already works), rather than structural-only validation — deliberately avoids touching `batakEasyAI`'s existing generic `pickRandom(legalMoves, rng)`.
- `docs/superpowers/plans/2026-07-15-batak-gomeli-rule-engine.md` — implemented as a diff to the existing `rules.ts`/`types.ts`/`rules.test.ts` (no new files), across 6 tasks. One deliberate, user-confirmed deviation from full generality: `fourCardCombinations` hardcodes "pick 4" instead of deriving from `kittySize`, since only one variant has a kitty today (YAGNI — flagged by review, kept as planned). Card-conservation testing was strengthened mid-review: the capstone integration test's original `Set`-size-only check couldn't distinguish "all cards present once" from "a card duplicated across two zones while another went missing" — fixed by adding a plain length assertion alongside it.

`batakGame`'s `RuleEngine` now handles both 4-player and 3-player gömmeli through the same code path, gated entirely by `players.length`. (At the time this sub-project shipped, gömmeli had no `AIStrategy` bidding/bury heuristics or mobile UI yet — both were completed in later sub-projects below, and gömmeli is now fully playable end-to-end.) Full 4-player behavior is unchanged and re-verified byte-for-byte (every existing test untouched, all passing).

**Phase 3 sub-project (3-player "gömmeli" AI strategies): complete, merged to `master` (2026-07-20).** Second of the two deferred gömmeli layers (rule engine was already done above) — fixes a real crash and makes all 3 difficulties bury/bid correctly in 3-player games. Scoped to the engine only; the mobile UI to actually reach this mode is still a separate, not-yet-started sub-project.

- `docs/superpowers/specs/2026-07-20-batak-gomeli-ai-strategies-design.md` — design spec. `docs/superpowers/plans/2026-07-20-batak-gomeli-ai-strategies.md` — 7-task implementation plan, executed via subagent-driven-development on branch `feature/batak-gomeli-ai-strategies` (fast-forward merged, no conflicts).
- **The crash**: every AI difficulty previously threw `TypeError: Reduce of empty array with no initial value` the instant any bot won the bid in a 3-player gömmeli game, because none of `batakEasyAI`/`batakMediumAI`/`batakHardAI` had a branch for the `'kitty-exchange'` phase — `chooseMove` fell through to card-play logic, filtered `legalMoves` for `'play'` moves (found none, since they're all `'bury'` moves), and called `.reduce()` on the empty result. Fixed identically across all three by adding a real `'kitty-exchange'` branch, each proven fixed by a test that drives a real 3-player state through `batakGame.getLegalMoves`/`validateMove` (not mocks) and would fail without the fix.
- **Two new pure heuristics** in `handStrength.ts`: `chooseCardsToBury` (discards non-trump cards first, lowest rank first within each group, falling back to trump only once non-trump runs out) and `scoreHandForBury` (the existing hand-strength score plus a length bonus — 0.5 per suit-card beyond the 4th — scoped to this function only, not leaked into bidding).
- **`estimateBidDecision`** (already shared by Medium/Hard) gained an optional `playerCount` param (default 4, so every existing call site is unaffected) and now reads its bid floor/cap from `ruleConstants(playerCount)` instead of hardcoded 4-player values — Easy AI still never competitively bids at all (per the existing easy-mode handicap), so this only affects Medium/Hard.
- **Easy AI**: calls `chooseCardsToBury` directly for the bury — this alone is the crash fix. **Medium AI**: same, plus passes `state.players.length` into its bidding call. **Hard AI**: the interesting case — card-play already uses `minimaxChooseMove`, so bury selection scores all C(20,4)=4845 possible bury combinations via `scoreHandForBury`, keeps the top 8, then runs a real `minimaxChooseMove` search over just that shortlist with a **dedicated** `trickMarginEvaluate` (raw `tricksWon` difference) at `maxDepth: 4`.
- **Two deliberate, verified corrections versus the original design spec**, both caught by tracing the real `minimaxChooseMove` code during planning, not left as written: (1) `maxDepth` is `4`, not the spec's original `3` — the top-level move here is the bury itself, not a trick-play, so a full 3-player trick (3 plies: lead, respond, respond) needs to fit inside `maxDepth - 1`; (2) the bury search cannot reuse the existing `calculateScore`-based evaluate the card-play search uses, because after only 1 simulated trick `calculateScore`'s bust-threshold quantization gives identical values for every bury candidate — no signal to compare by. Both corrections were independently re-derived against `packages/engine/src/ai/minimax.ts`'s actual ply-accounting by two separate reviewers (the task-level reviewer and the final whole-branch reviewer) and confirmed correct, not just asserted.
- **`simulateGames` end-to-end proof**: 500 easy-vs-easy-vs-easy 3-player games with zero card-conservation violations across the new `kitty`/`buried` zones, and Hard AI beat 2×Easy in 136/200 games (68%) — comfortably above the coarse `>80` sanity floor and well above the ~33% pure-chance baseline for 3 players. Notably much stronger than the already-known, still-unfixed 4-player Hard-vs-Easy rate (49/200, ~chance-level) — the bury/kitty advantage plus only two opponents materially helps Hard AI here, a good sign for the eventual bidding-recalibration work but not itself a fix for it.
- Every task passed its own task-scoped review clean (no Critical/Important findings across all 7); the final whole-branch review was also clean. Minor, accepted, non-blocking notes only: a bury-cardIds-to-tuple type cast (`as [string,string,string,string]`) is now duplicated across `easy.ts`/`medium.ts`/`hard.ts` (safe today since `kittySize` is structurally always 4 whenever `'kitty-exchange'` is reachable; worth a shared helper if ever touched again); `state.trumpSuit!` non-null assertions still lack an inline "why safe" comment; a couple of `chooseCardsToBury`/`scoreHandForBury` edge cases (`buryCount` exceeding hand size, an all-trump hand) are handled correctly but untested, since neither is reachable under gömmeli's 20-card kitty-exchange hands.
- `getGame('batak')`'s AI strategies now handle bidding/bury correctly for both 4-player and 3-player gömmeli through the same `AIStrategy` objects. (At the time this sub-project shipped, gömmeli still had no mobile UI — that sub-project is now complete, see below; gömmeli is fully playable end-to-end.)

**4-player Batak deal sequence & hand UI polish: complete, merged to `master` (2026-07-15).** User-requested changes to the already-playable 4-player table, brainstormed with the visual companion for the shuffle-animation style. Scoping research surfaced a real pre-existing gap folded into the same fix: the human's hand was previously invisible during bidding/trump-selection (only rendered once `phase === 'playing'`) — also what made "reveal trump after hands are shown" true for free once fixed.

- `docs/superpowers/specs/2026-07-15-batak-deal-sequence-and-hand-ui-design.md` — a stylized "scatter" shuffle (not a literal riffle/overhand simulation) plus a cut, chosen over two other animated mockups shown via the visual-companion browser tool; an additional 1.2s pause after the animation, before reveal. The human's hand is now sorted (Hearts→Spades→Diamonds→Clubs, then rank descending, gap-free for missing suits) and rendered as an always-rebalancing two-row curved fan (7/6 split for 13 cards) at `PlayingCard`'s `'small'` size — `'normal'` size literally doesn't fit two rows of 7 within a phone-width screen without scrolling, which would have defeated the point. Applies to the human's hand only; opponents' face-down fan/stack is untouched.
- `docs/superpowers/plans/2026-07-15-batak-deal-sequence-and-hand-ui.md` — 2 tasks (hand sorting/layout/visibility; the deal animation + entrance) plus a manual visual-verification pass. `SelectableCard` (shared with Pişti) gained one new optional `curveOffsetY` prop, additive/backward-compatible. Two real bugs were caught and fixed before merging, neither by the task-level code reviews alone:
  - **Task 2's own review** caught a genuine accessibility bug baked into the plan's own prescribed code: `useDealSequence`'s effect had an empty `[]` dependency array, so it always captured a stale `reducedMotion === false` on first render and never re-ran once the async `AccessibilityInfo` check resolved — OS reduce-motion users would still have seen the full ~2.9s animation every hand. Fixed by adding `reducedMotion` to the dependency array.
  - **The manual visual-verification pass** (browser/Playwright screenshots + computed-style checks, not just typecheck) caught a cosmetic-but-real issue no code review could: the deal overlay's backdrop color was nearly identical to the table's green background, so it didn't visually read as "covering" the table even though it correctly blocked touches. Fixed by darkening it to a dark navy matching the app's existing card-back palette, then re-verified with fresh screenshots (not just re-typechecked).

Sort order and two-row layout were verified directly from screenshots (not just code) at two different hand sizes, both correct. Row-rebalancing after a card is played was **not** empirically screenshotted (an automated-script timing issue, not a product bug) — verified by code review only (`splitTwoRows` is a pure function recomputed from the current hand length every render).

**Known issue, needs investigation (2026-07-15, updated same day): Batak's bidding AI (Medium/Hard) appears too aggressive and unreliable.** User reported bots bidding 10 or 12 in 4-player games and then failing to fulfill the contract, across two games in a row — "very very unlikely" for a well-calibrated heuristic. `estimateBidDecision`/`handStrength.ts` (`packages/engine/src/games/batak/ai/`) is the likely place to start. **Now empirically confirmed, not just anecdotal**, as a side effect of the Easy-mode work below: with Easy AI never competing for the bid, Hard AI always ends up with the contract in `simulate.test.ts`'s 4-player Hard-vs-3×Easy benchmark — and its win rate over 200 deterministic games dropped to 49/200 (24.5%), statistically indistinguishable from the ~25% pure-chance baseline. This is a real, measured data point (not a one-off anecdote) that Hard's bidding heuristic is overbidding relative to what it can actually make. Deliberately still not fixed — the user explicitly chose to defer this to a future session again rather than scope-creep the Easy-mode work — but `simulate.test.ts`'s previous `hardWins > 70` assertion was lowered to `hardWins > 30` (a coarse "hasn't totally collapsed" floor, not a real quality bar) with an inline comment pointing back here; **restore that threshold toward `>70` as part of whatever fixes this**.

**Batak Easy difficulty: bots never bid + guaranteed strong human hand, complete, merged to `master` (2026-07-15).** Two related changes making Easy mode more learnable for beginners, requested directly by the user (not derived from the aggressive-bidding issue above, though it surfaced the finding noted above as a side effect):
- `docs/superpowers/specs/2026-07-15-batak-easy-mode-handicap-design.md` — design spec. `docs/superpowers/plans/2026-07-15-batak-easy-mode-handicap.md` — 3-task implementation plan, executed via subagent-driven-development on branch `game/batak-easy-mode-handicap` (fast-forward merged, no conflicts).
- **Easy AI's bidding was first fixed from pure-random to hand-strength-based** (reusing the same `estimateBidDecision`/`chooseTrumpSuit` Medium already uses, plus a "always play lowest legal card" basic card-play strategy replacing full randomness) — committed separately on `master` before this branch, since it was a standalone bug fix the new feature then built on top of. Root cause: `batakEasyAI.chooseMove` previously called `pickRandom(legalMoves, rng)` for every phase including bidding, where legal moves are every integer from the bid floor to the cap — completely decoupled from the bot's actual hand.
- **Task 1 (engine):** new opt-in `BatakSetupOptions.guaranteeStrongHand?: boolean`. When true, `batakGame.setup()` reshuffles (reusing the same seeded `RNG`, capped at a 10,000-attempt safety valve) until `state.players[0]`'s dealt hand satisfies an AND'd predicate: (2+ Aces OR 3+ Kings OR 1+ Ace+2+ Kings) AND (some suit with 5+ cards OR some suit holding all of A/Q/J/10). Chosen over constructively building a qualifying hand because it reuses all the existing, already-tested dealing code and guarantees the result is still a genuinely possible random deal. `players[0]` is reused as "the human seat" rather than introducing a second convention, since the engine's forced-all-pass rule already hardcodes that assumption.
- **Task 2 (engine):** `batakEasyAI`'s bidding branch now returns `{ type: 'pass' }` unconditionally, regardless of hand or the current highest bid — Easy bots never competitively bid at all. Combined with the forced-all-pass rule already assigning the contract to `players[0]` when everyone passes, and the mobile app always seating the human at `players[0]`, the human always ends up as `bidWinner` in an Easy game — even if they themselves choose to pass, they get forced into the contract via the existing fallback rather than a bot ever picking it up (confirmed via code trace in the final whole-branch review, not just asserted).
- **Task 3 (mobile):** one-line wiring in `BatakScreen.tsx` — `guaranteeStrongHand: difficulty === 'easy'` passed into the `setup()` call.
- Trump-selection and card-play logic (lowest-legal-card) for Easy are unchanged from the prior bug fix above.
- Mid-branch process note: Task 2's implementer correctly stopped and escalated (rather than silently loosening a test threshold) when this change caused the `simulate.test.ts` regression described in the known-issue paragraph above — a good example of the "escalate rather than guess" instruction working as intended.
- Final whole-branch review: ready to merge, no Critical/Important findings. Minor, non-blocking notes: the `simulate.test.ts` threshold is now a coarse sanity floor rather than a real quality signal (tracked above); the test file's predicate helpers intentionally duplicate `rules.ts`'s private ones as an independent oracle (could drift if the production predicate is retuned — a one-line "mirrors rules.ts" comment would guard against this, not done); `guaranteeStrongHand`'s genericity for the 3-player gömmeli hand size (16 cards) was verified correct by code trace but has no dedicated test, since gömmeli has no UI yet.

**Batak table UI polish pass (2026-07-16): complete, merged to `master`.** A 7-item user-requested pass on the 4-player Batak table, separate from (and not derived from) the Alper Games reference-checklist work Pişti went through — this one was its own brainstormed spec (`docs/superpowers/specs/2026-07-16-batak-table-ui-polish-design.md`) and 8-task plan (`docs/superpowers/plans/2026-07-16-batak-table-ui-polish.md`), executed via subagent-driven-development on branch `game/batak-table-polish` (fast-forward merged, no conflicts).
- **Wood edge rails**: new `packages/ui/src/TableEdgeRails.tsx` (straight wood-textured rails along all 4 table edges, matching `TableWoodCorners`' gradient/grain/gold-trim recipe), inset to meet the existing 4 corner wedges via a newly-exported `CORNER_WEDGE_SIZE`. Batak-only per the standing rule about table-visual changes — not mirrored into Pişti.
- **AI 2 (top seat) fan tightened**: replaced a fixed-pixel overlap (`OPPONENT_CARD_OVERLAP=21`, ~39%) with the same percentage-overlap approach the side stacks already use, now 85% — visually consistent density across all three AI seats.
- **Human seat reversed**: the "You" badge now renders above the hand (closer to the middle row) instead of below it; the hand itself now hugs the bottom screen edge. Pure JSX reorder, no logic change.
- **Human hand flattened + width-filling**: `fanRotationDeg`/`fanCurveY` (`apps/mobile/src/table/seating.ts`) gained optional `degreesPerStep`/`curveMultiplier` params (defaulted to the original constants, so Pişti and the AI-2 seat are byte-identical); the human hand now uses a flatter slope and a new `fillWidthMarginPx` helper that spreads each row to fill ~70% of the actual measured hand-area width (15% gutter each side, `useWindowDimensions`-seeded + `onLayout`-corrected), replacing the old fixed-percentage `HUMAN_HAND_OVERLAP_PERCENT` (removed, now fully dead).
- **Selected card now renders in front of every other card** (previously deliberately rendered *behind*, as a workaround for a stray-tap bug from the immediately preceding session). Made safe via a new `hitSlop` prop on `SelectableCard` that shrinks only the selected card's touchable bounds — not another z-order trick — plus a subtle scale-up (1.05×) added to the existing lift animation so selection reads as lifting toward the camera.
- **Tap-empty-area-to-deselect**: `BatakTable`'s outer container is now a `Pressable` calling the existing `clearSelection`; standard RN touch-responder negotiation means nested Pressables (cards, bid/trump buttons) still claim their own taps first.
- **New "Dim Unplayable Cards" setting**: global (`settingsStore.dimUnplayableCards`, default `true`), toggled via a new gear button in `GameScreenLayout`'s header (opt-in via a new `onSettingsPress` prop — Pişti's screen doesn't pass it, so its header is unchanged) and a new one-toggle `BatakSettingsModal`. Gates only `SelectableCard`'s visual scrim, never the underlying tap-blocking.
- A pre-existing, unrelated cosmetic reformat sitting uncommitted in `seating.ts` (single→double quotes, line-wrapping, present before this session started) was accidentally discarded by one task's implementer while it was "isolating" its own diff, with no stash/commit to recover it from — reconstructed manually and re-committed once the loss was caught and flagged; **lesson recorded for future sessions: instruct implementers to use targeted edits only, never a full-file rewrite, when a file has unrelated pending changes, even when trying to keep their own commit's diff clean.**
- Verified via the browser/Playwright workflow (`[[dev_sandbox_no_device_access]]`) end-to-end through a real Easy-mode game (deal → bid → forced-all-pass → trump selection → playing phase on a genuinely partially-illegal hand, confirming the dim toggle against real trump-suit-unbroken illegality, not a synthetic case). Zero console errors. **Still no native on-device verification** — same standing gap as every prior Batak/Pişti UI pass.
- Final whole-branch review: ready to merge, no Critical/Important findings. Minor, non-blocking notes: the global dim-toggle, once switched off in Batak, also affects Pişti's rendering for the rest of that app session (no gear button there to restore it, no persistence) — accepted as-is per the user's explicit call; wood palette/derivation now duplicated across `TableWoodCorners`/`TableEdgeRails` (folded into the already-tracked navy/gold theme-extraction pass); `handFanRow`'s comment describes cross-row z-index stacking slightly imprecisely (the real mechanism is render-order, not zIndex crossing rows — cosmetically correct today, worth tightening if the row structure ever changes); measured human-hand gutter came out ~13%/13% vs the 15% target (accepted first-pass tuning). Per the standing table-visual-change rule, whether Pişti should get any of these treatments (edge rails, flatter hand, front-stacked selection) was explicitly raised with the user, who chose not to mirror it now — revisit as its own future discussion, not automatically.

**Card corner-index & AI hand-layout polish (2026-07-17): complete, merged to `master`.** User-supplied reference screenshots (`docs/references/batak-and-card-references/`: Alper Games Batak, a "current Batak" screenshot, and a 10-text-and-glyph-alignment closeup) drove a 3-part fix, spanning both Pişti and Batak — spec at `docs/superpowers/specs/2026-07-17-card-layout-and-typography-polish-design.md`, plan at `docs/superpowers/plans/2026-07-17-card-layout-and-typography-polish.md`, executed via subagent-driven-development on branch `ui/card-layout-and-typography-polish` (4 tasks, each reviewed clean with zero fix cycles; fast-forward merged).
- **Corner rank/suit optical centering** (`packages/ui/src/PlayingCard.tsx`, shared — benefits Pişti, Batak, and `apps/playground` automatically): the prior fix for cross-card consistency (`alignItems: 'flex-start'`, shrink-wrapping to the widest child) reintroduced the complementary same-card problem the user flagged — "10" (the only two-character rank) visually drifted from its own suit glyph below it. Replaced with a **fixed-width** index container (`CORNER_INDEX_WIDTH = {normal: 28, small: 19}`) plus `alignItems: 'center'`, getting both properties at once: every rank's text and glyph share a center axis (same-card), and that axis sits at the same absolute offset across every rank (cross-card) — no per-rank lookup table needed. Verified across several ranks including "10" itself via the browser/Playwright workflow.
- **AI opponent hand — flat, auto-fit spacing** (`apps/mobile/src/table/seating.ts` + both games' table files): opponent hands (top single-seat row, left/right side stacks) previously used static, per-game-tuned overlap percentages plus rotation/curve on the top seat, which read as messy. Reused the existing `fillWidthMarginPx` helper (already built for the human hand's width-fill) for opponent spacing too, measured via `onLayout` (top row width, side-stack height) — one continuous formula that spreads a small hand (Pişti's max-4 opponent cards) into a clean evenly-gapped row and compresses a large hand (Batak's 13-card start) into tight overlap automatically, exactly matching the user's stated preference order without needing two separate modes. Rotation/curve dropped entirely for opponent seats (the human/"You" hand's own fan is untouched in both games). This left a pile of static constants dead (`OPPONENT_CARD_OVERLAP`, `SIDE_CARD_STYLES`, Batak's local overlap-percent constants, `overlapMarginPx` itself) — all removed in a dedicated cleanup task once confirmed repo-wide unused.
- **Wider human-hand margin, Batak only**: `HUMAN_HAND_SPREAD_FRACTION` bumped `0.7 → 0.92` (15% gutter each side → ~4%), matching the reference much more closely. Pişti's human hand needed no change — it's a plain centered max-4-card row that never had the conservative-margin problem.
- Two independent task-implementers built the Pişti and Batak opponent-fan rewrites separately (different files, same brief pattern); the final whole-branch review confirmed they converged on identical prop names/types/constants with no unexplained drift.
- No new automated tests, per the standing 2026-07-07 mobile-UI testing policy — full existing suite (39 suites/229 tests) re-verified passing on the merged result.
- Verified via the browser/Playwright workflow (`[[dev_sandbox_no_device_access]]`): corner-index alignment across many ranks (A, 5, 7, 9, 3, 4, 6, K, Q, J, and — after a few reload retries — 10 itself); Pişti at both a full 4-card opponent hand and a played-down 1-card endgame hand; Batak at both the full 13-card start and mid-hand at 11 cards with live trick state; zero new console errors/warnings. **Still no native on-device verification** — same standing gap as every prior UI pass.
- Final whole-branch review: ready to merge, no Critical/Important findings. 2 Minor, both accepted tradeoffs: side seats call `useWindowDimensions()` unconditionally though only the top seat reads it (negligible extra re-renders on orientation change); the 4 new spacing constants + `SMALL_CARD_WIDTH`/`HEIGHT` are duplicated byte-identically across `PistiTable.tsx`/`BatakTable.tsx` (consistent with this codebase's deliberate per-game-table-independence precedent, folded into the already-tracked future theme/constants-extraction pass).

**Card face polish + Batak turn indicator (2026-07-18): complete, merged to `master`.** A small, ad-hoc 3-item pass (not part of the reference-checklist or table-polish sequences above), brainstormed with the visual companion for the spade/club redesign, then executed via subagent-driven-development on branch `ui/card-face-polish-batak-turn-indicator` (spec: `docs/superpowers/specs/2026-07-18-card-face-polish-and-batak-turn-indicator-design.md`, plan: `docs/superpowers/plans/2026-07-18-card-face-polish-and-batak-turn-indicator.md`; fast-forward merged, no conflicts).
- **Court-card frame vertical alignment** (`packages/ui/src/CourtCardFrame.tsx`): the Q/K/J decorative bracket frame previously sat at a fixed 13%-edge-inset unrelated to where the corner index actually renders. Recomputed from `PlayingCard.tsx`'s real layout constants and moved to the corner index's suit-glyph vertical center (37px from top/bottom at `normal` size, 26px at `small`) — deliberately not aligned to the rank letterform itself, since K/Q/J share the same suit-glyph position but different letter shapes.
- **Spade/club redesign** (`packages/ui/src/SuitIcon.tsx`): replaced with shapes matching two user-supplied reference photos (`docs/references/card-art/SPADE.jpeg`/`CLUB.jpeg`) — a classic convex spade (leaf + separate flared stem, replacing the previous concave-"waisted" design) and a club with widened, closer-overlapping circles (removing the previous punched-out white center hole, which is no longer needed once the circles close their own gap). Hearts/diamonds untouched. Went through several rounds of visual-companion iteration — a wider base, then a longer "wing-flare" stem, then a shape traced pixel-for-pixel from a user-supplied paint-sketch (`docs/references/card-art/paint-sketch-spade.png`, measured via a small PNG-decoding script rather than eyeballed) — before the user asked to revert to the very first candidate shown, which is what shipped. Recorded in the spec so a future session doesn't retread the same rejected alternatives.
- **Batak turn indicator simplified** (`apps/mobile/src/games/batak/BatakTable.tsx`, Batak-only — Pişti explicitly out of scope): removed the gold `activeArea` background wash and the face-down opponent hand-card stacks entirely; the pre-existing glowing green `PlayerBadge` (already driven by the same `isCurrentTurn`/`isHumanTurn` booleans) is now the sole turn indicator. Cascaded into a real dead-code cleanup — `sideStackHeight` prop threaded through `OpponentSeat`/`OpponentSeatGroup`/3 call sites, the `middleRowHeight` measurement state/handler, 6 spacing constants, 2 styles, and an unused `useMemo` import all removed once their only consumer (the deleted card-stack rendering) was gone.
- No new automated tests (decorative UI, per the standing 2026-07-07 policy) and no proactive screenshot/visual verification (per the user's 2026-07-17 direction) — full existing suite (39 suites/229 tests) re-verified passing on the merged result. Final whole-branch review: ready to merge, no Critical/Important findings. 2 Minor, both accepted/non-blocking: the frame aligns to the SuitIcon box center rather than the redesigned glyphs' visual ink-center (cosmetically negligible, matches the spec's explicit scoping); the Batak opponent-seat layout collapse (card stacks gone) is the most visually consequential change in the branch and is flagged as worth an opportunistic on-device glance whenever native verification next happens — **still no native on-device verification, same standing gap as every prior UI pass.**
- **Follow-up fix, same day: court-card frame alignment had a real bug, found via root-cause debugging.** The merged version above aligned the frame to the suit glyph's own center; the user reported it still looked off and asked for "vertical" alignment instead, which — after a clarifying question — meant centering on the whole rank+suit index block, not the glyph alone. That alone wasn't the full story: after implementing it, the user tested again and said the block still wasn't perfectly centered. Rather than guess again, spun up the browser/Playwright workflow specifically for this diagnosis (an explicit, one-off exception to the "no unsolicited screenshots" rule, done with the user's consent) and measured real DOM pixel positions via `apps/playground`'s card gallery. Found a genuine root cause: `CourtCardFrame`'s SVG declares its coordinate canvas as the full outer card size, but actually renders inside `PlayingCard`'s bordered content box, which is 4px shorter (two 1px border rings) — so any Y value *mirrored* from a top-anchored one (`boxHeight - topValue`) was using the wrong height (132/86 instead of the real 128/82), producing a confirmed ~4px error on the bottom bracket and a ~1.5px overlap with the opposite glyph on the top bracket's vertical segment. Fixed the height constant, verified the corrected math landed exactly on the real measured glyph-block midpoint (to the decimal), then the user hand-tuned all four bracket paths and both `strokeWidth`s against a live render for the final look — `GEOMETRY`'s current values are visually-verified ground truth, not a pure formula output; the file's comments were rewritten to say so explicitly, so a future session doesn't try to "correct" them back to the computed baseline.

**Wooden hand-frame + green felt background (2026-07-18): complete, merged to `master`.** Batak-only for now (Pişti explicitly deferred — same standing per-game visual-change rule as always). Spec: `docs/superpowers/specs/2026-07-18-hand-wooden-frame-design.md` (rewritten after the fact to match what actually shipped, since this went through live implementation-and-iteration against the running app rather than an upfront plan). New `packages/ui/src/HandFrame.tsx` renders `wooden-frame-long-Photoroom.png` behind Batak's two-row hand, peak-aligned to the top row's own curved peak (`fanCurveY`) with a 14dp reveal margin so the gold trim clears the cards instead of hiding behind them, height-stretched (`resizeMode="stretch"`, width never touched) to reach flush past the true screen bottom, and horizontally overscanned to compensate for a ~2.3% transparent margin baked into the asset itself (measured from its alpha channel, not assumed). `TableFelt` now renders `green.png` instead of a hand-drawn SVG weave — applies to Pişti too, per explicit direction ("green.png for all tables"). `TableEdgeRails`/`TableWoodCorners` both gained optional `edges`/`corners` props (default: all, no behavior change elsewhere) so Batak can drop its bottom rail and bottom-corner wedges in favor of the frame. Batak's two-row hand now overlaps 25% instead of a fixed gap. The asset itself went through 4 real, sequential problems before landing — a flat JPEG with a checkerboard baked in as opaque pixels (no real alpha), a mockup that used illustrative proportions instead of Batak's real numbers (masked a genuine "asset not tall enough" shortfall), and two rounds of asset replacement — each caught by measuring real pixel/alpha data or checking real device dimensions, not by eyeballing. No new automated tests (decorative UI, per the standing 2026-07-07 policy); verification was live in the user's own running Expo session via Fast Refresh, not proactive screenshots.

**Shared card-travel animation, Batak only (2026-07-18): complete, merged to `master`.** Spec: `docs/superpowers/specs/2026-07-18-shared-card-travel-animation-design.md`, plan: `docs/superpowers/plans/2026-07-18-shared-card-travel-animation.md`, executed via subagent-driven-development on branch `feature/shared-card-travel-animation` (1 task, fast-forward merged, no conflicts, task review + final whole-branch review both clean with zero findings). Batak's existing local `TravelCard` (which already had the right shape — it only animates an origin-offset down to rest, letting its parent position the actual destination) extracted into `apps/mobile/src/table/TravelCard.tsx` as a shared component (`{ originOffset, resetKey, children }`), and Batak's one call site rewired to use it — a pure, pixel-identical extraction. Pişti's own separate `RevealCard` (scale-in, label, baked-in destination offset) is untouched and explicitly out of scope. Internally factored the reset/timing lifecycle into a non-exported `useTravelProgress` helper specifically so a future consumer needing the raw progress value (see below) can promote it to an exported hook with a one-line change instead of a refactor.

**Known gap, not yet started: the human hand's card-travel origin is a fixed generic offset, not the card's real position.** Surfaced live (2026-07-18) right after the extraction above shipped: `apps/mobile/src/table/seating.ts`'s `resolveRevealOrigin`/`revealOriginOffset` — pre-existing, unchanged by the extraction — always resolves the human's own plays to a single fixed `{x:0, y:195}` "bottom" vector, regardless of which of the 13 hand cards (in which row, which fan position) was actually played. This reads fine for opponents (no per-card visual exists for them anyway, since the 2026-07-18 turn-indicator-simplification pass removed the face-down opponent card stacks entirely — only a badge remains) but is visibly wrong for the human's own hand, where the player has a precise expectation of which card should move from where. This does NOT satisfy the original ask this whole sub-thread was about ("every card should animate from its own original position") for the human specifically — Approach 1 (the shared-component extraction above) deliberately preserved the pre-existing fixed-direction behavior rather than solving this, and that gap wasn't caught until live testing.

Mid-brainstorm on the fix, not yet decided or spec'd. Three approaches discussed: (A) compute the origin analytically from the same deterministic layout formulas already used to render the hand (cheap, no new measurement plumbing, but the destination — the trick center — sits inside a `flex: 1` region whose real height depends on total screen height in a way that's fragile to compute exactly without measuring); (B) measure both ends for real via React Native's `measureInWindow` (robust, sidesteps the flex-region problem entirely, more plumbing); (C) hybrid — cache the trick center's destination position once via `onLayout` (cheap, stable, matches this codebase's existing `middleRowHeight`-style pattern), measure only the played card's real position live at tap time. Leaning toward C as the recommendation, but the user hasn't chosen yet — **pick this up first in the next session** before moving to any of the "Next up" candidates below.

**Also outstanding, unrelated to any of the above:** an uncommitted change was found sitting in the working tree on `apps/mobile/src/state/settingsStore.ts` (quote-style reformat — single→double quotes, arrow-fn parens removed — plus `dimUnplayableCards: true` → `false`) and a stray untracked `jest-output.txt` at the repo root. Neither was made by Claude this session; flagged to the user once, no response yet, left untouched (not committed, not discarded). Worth resolving/asking about before it's forgotten.

**3-player gömmeli Batak mobile UI: complete, merged to `master` (2026-07-21).** Makes gömmeli genuinely playable by a human end-to-end for the first time — rule engine and AI strategies were already done (see the sub-projects above); this was the last remaining layer. Spec: `docs/superpowers/specs/2026-07-21-batak-gomeli-ui-design.md`, plan: `docs/superpowers/plans/2026-07-21-batak-gomeli-ui.md`, executed via subagent-driven-development on branch `ui/batak-gomeli-mobile-ui` (10 tasks, all independently reviewed clean).
- Setup screen gained a Standard/Gömmeli variant picker (`BatakSetupView.tsx`, new `batakVariant.ts`); `BatakScreen.tsx` became variant-aware (`AI_IDS_BY_VARIANT`/`PLAYER_NAMES_BY_VARIANT`, `isTrickCompleting` generalized from a hardcoded `=== 3` to `state.players.length - 1`); `assignSeats` gained a 2-opponent (left/right) branch; a new `KittyPile`/`kittyPileCards` renders the permanently-visible face-down kitty from deal onward.
- The bulk of the work: a fully staged, animated kitty-exchange bury interaction. `useBurySlots` (new hook) + `BurySlots`/`KittyExchangeCenter` (new components) let the human bidder tap cards from hand into 4 placeholder slots (real `TravelCard` hand↔slot animation, not a snap) and Confirm; `BatakScreen.tsx`'s new `pendingBury` state machine then stages the reveal purely in the UI (no engine-call reordering) — buried cards fly from the slots to the pile (`GatherCard`, unchanged existing component), the original kitty flips face-up and holds for ~3.5s (new `KittyRevealCard`), then flies out to the bidder's hand (new `KittyCollectCard`, `GatherCard`'s shape minus the flip since these cards are already revealed).
- **Final whole-branch review caught one real Important finding**, fixed and re-reviewed clean before merge: the AI bidder's 4 discarded cards briefly flashed face-up (~265ms, half of `GatherCard`'s 530ms travel) during the "burying" animation, because `GatherCard` starts face-up by design (correct for the human path, who already saw those cards in the slots) — a genuine information leak for an AI bidder, since the whole point is hiding which cards the AI buried. Fixed by branching the burying-stage render on `isHumanBidder`: `GatherCard` unchanged for human, a new `faceDown` prop on `KittyCollectCard` for AI. Also wired `useBurySlots`'s hardcoded `4` to `ruleConstants(3).kittySize` (a Minor recommendation from the same review).
- **Two more real bugs found via the user's own live playtest after that merge-ready point, fixed in a follow-up commit**: (1) the 4 kitty cards were invisible in the human's hand after the exchange completed — `BatakTable.tsx`'s `handLayerRef` (assigns each card a fixed top/bottom fan row) only ran its assignment once, at the initial deal, so a card joining the hand later (the kitty merge) matched neither row's filter and silently never rendered; fixed with a general "assign any newly-seen card to whichever row has fewer cards" branch. (2) the hand overflowed the screen at gömmeli's larger row sizes (16 steady-state / 20 mid-exchange vs. Standard's 13) — `HumanHandFan.tsx`'s fixed per-card overlap was tuned for Standard's max row of 7; added a `compact` prop (gömmeli-only, via `opponentPlayerIds.length === 2`) with tighter overlap fractions (0.75/0.68 vs. 0.5/0.4), first-pass values worth an opportunistic re-tune.
- No new automated UI tests, per the standing 2026-07-07 policy. **Still no native on-device verification** — same standing gap as every prior Batak/Pişti UI pass; per the user's 2026-07-17 direction, no proactive screenshot verification was done either — all bug-finding this round came from the user's own live Expo testing.

**Batak gömmeli blind-bury rule change: complete, merged to `master` (2026-07-21), same day as the UI above.** A real rule correction the user asked for after playing the just-built UI live: the bidder must choose their 4 bury cards from their **original 16-card hand only** — a kitty card can never legally be buried, for AI bidders too, not just human. Plan (spec skipped, per explicit user choice, since the requirement was already precisely nailed down through a multi-round clarifying-question exchange): `docs/superpowers/plans/2026-07-21-batak-gomeli-blind-bury.md`, executed via subagent-driven-development on branch `game/batak-gomeli-blind-bury` (based on the UI branch above, before either was merged — both merged together in one commit).
- **Every fixture and expected value in the plan was verified by directly prototyping the fix and running it against the real engine/AI code before any task was dispatched** (not hand-derived/guessed) — the prototype was then reverted so implementers applied the same, now-known-correct changes fresh through the normal review process. This included a delicate hand-traced Hard-AI minimax proof (`hard.test.ts`) that needed restructuring once 4 inert kitty cards were added to the test hand without disturbing the original trick-outcome logic.
- New `buriableCards(state, playerId)` in `rules.ts` — excludes `state.kittyCardIds` from the hand, falls back to the full unfiltered hand when `kittyCardIds` is `null` (the only way 4-player Standard Batak and Pişti stay completely untouched, verified via byte-identical passing suites). `kittyExchangeLegalMoves` now calls it; `validateMove`'s `'bury'` case already reused `getLegalMoves` as its source of truth, so this one fix also fixes validation — a kitty-card bury is now rejected even if a future UI bug tried to submit one.
- All 3 AI difficulties (`easy.ts`/`medium.ts`'s `chooseCardsToBury` call site, `hard.ts`'s `fourCardCombinations` candidate generation) updated to consume `buriableCards` instead of the raw hand — proven correct not just by unit tests but by the full 500-game 3-player gömmeli `simulateGames` benchmark, which would otherwise crash with "AI chose an illegal move."
- Mobile UI counterpart: `BatakTable.tsx`'s `kittyExchangeHiddenCardIds` (previously only hid kitty cards from the human's hand fan *after* Confirm) now hides them for the *entire* kitty-exchange phase whenever the human is the bidder — the old narrower nested condition was proven strictly redundant once the wider check was added (independently re-derived by the task reviewer against `BatakScreen.tsx`'s actual `pendingBury` timing, not just accepted) and removed rather than left as dead code. No other file (the staged reveal/collect animation components, `useBurySlots`, `KittyPile`) needed any change.
- Final whole-branch review: ready to merge, no Critical/Important findings — only 2 pre-existing Minor notes (already tracked from prior sessions: the bury-cardIds tuple-cast duplication across the 3 AI files; unannotated `trumpSuit!` non-null assertions).

Gömmeli Batak (3-player, buried-kitty) is now fully playable end-to-end by a human, with the blind-bury rule correctly enforced for every difficulty.

**Animation Playground (`apps/playground`, animation mode): complete, merged to `master` (2026-07-23).** A new, fully self-contained "Animation Playground" mode inside the existing `apps/playground` app (reached via a Design/Animation toggle in `App.tsx`) — an isolated lab for perfecting card-travel animation quality before any of it is ported into Batak, per `apps/playground/ANIMATION_ARCHITECTURE.md`/`CLAUDE_ANIMATION_RULES.md` (two docs the user placed directly in the repo as the standing source of truth for this sub-project's philosophy and working method). Spec: `docs/superpowers/specs/2026-07-23-animation-playground-design.md`. Plan: `docs/superpowers/plans/2026-07-23-animation-playground.md` (8 tasks), executed via subagent-driven-development on branch `feature/animation-playground` (fast-forward merged, no conflicts).
- **Deliberately does not use `react-native-reanimated`** — confirmed with the user before writing any code: nothing else in this repo uses Reanimated (every existing Pişti/Batak animation runs on React Native's built-in `Animated` API with `useNativeDriver: true`), so this playground stays on `Animated` too, keeping an eventual Batak port a drop-in rather than a second animation system to adopt repo-wide.
- **Fully self-contained module** (`apps/playground/src/animation/`): its own simplified `SimpleCard` (plain rank text + Unicode suit glyph, one fixed size — no card art, no `@world-cards/ui` dependency, per the architecture doc's explicit "Playground Scope").
- **Core deliverable: `useCardMotion`** (`engine/useCardMotion.ts`) — one `Animated.Value` per card driving every visual output (`translateX/Y`, `rotate`, `scale`, `glyphScale`) via `.interpolate()`, with a `retarget()` method that always starts the next leg from the card's *actual current* interpolated position (read via an `Animated.Value.addListener` cached in a ref), never the original layout position — the "single source of truth" / "preserve spatial continuity" rules from the architecture doc, implemented literally rather than approximated.
- **All 6 demos from the architecture doc, one per file**: Demo 01 (configurable fan layout, live sliders), Demo 02 (tap-to-lift selection, rotation held fixed), Demo 03 (tap-to-play travel, begins from the current lifted position), Demo 04 (easing/landing comparison, 4 live-switchable presets), Demo 05 (scale/glyph transformation, single timeline), Demo 06 (everything combined: a continuous 4-seat deal loop via a new `useDealLoop` reducer built on `@world-cards/engine`'s `createDeck`/`shuffle`/`createRng`, human seat interactive, 3 AI seats auto-playing on a timer, infinite reshuffle once all hands empty). Deliberately no bidding/trump/trick-winner logic anywhere — confirmed with the user — `useDealLoop`'s turn order is a fixed `0→1→2→3→0` rotation forever, purely to generate a continuous stream of realistic play events to animate.
- **Two real bugs found via task review, both fixed and re-verified, both inherited from the plan's own prescribed code rather than implementer-introduced** (the plan document itself was patched in both cases so later tasks didn't transcribe the same mistake): (1) Demo 03's tap-to-play state machine could permanently freeze a card if tapped again mid-`'traveling'`/`'holding'` — fixed with a one-line guard, proactively patched into Demo 04/05's not-yet-implemented plan text before those tasks were dispatched; (2) Demo 06's human-played card animated in from the wrong direction (backwards Y-sign, inherited from misapplying a different demo's same-component coordinate convention to `TrickCard`'s separately-positioned reference frame) — fixed with a one-line sign correction, verified via an actual `playwright-core` screenshot sequence showing monotonic rise toward the trick slot across frames, not just code reading.
- Task 4 (`useCardMotion`) got deliberately extra review scrutiny as the primitive every later task depends on — the retarget-captures-live-position ordering, the listener lifecycle, and the `generation`-bump `useMemo` rebuild pattern were all independently traced and confirmed correct, not just visually inspected.
- No automated tests written anywhere in this branch, per the standing 2026-07-07 mobile-UI testing policy — every task verified via typecheck plus, for several tasks, genuine manual runs via `expo start --web` + `playwright-core` against the sandbox's real Chrome (reusing the established `[[dev_sandbox_no_device_access]]` workflow).
- Final whole-branch review: ready to merge, no Critical/Important findings. Minor, non-blocking notes only: `resetTimer` has no unmount-cleanup effect across Demo 03/04/05 (same gap consistently applied, acceptable for a prototyping tool judged by eye); `DealSeatState` (in `types.ts`, from the Task 1 scaffold) is a dead export, never consumed; the demo switcher relies on `DemoId` union exhaustiveness rather than an explicit `default` case (fine, just don't "helpfully" add one back).
- **Directly relevant to the still-open Batak gap** noted below (the human hand's card-travel origin being a fixed generic offset, not the card's real position) — this playground's `useCardMotion`/`retarget()` pattern is exactly the kind of "begin from the actual current position" primitive that gap needs, but porting it into Batak is explicitly out of scope for this sub-project and not yet started.

**Animation Playground Demo 1–3 refinement pass (2026-07-26): mostly complete, NOT yet committed.** A user-driven, conversational iteration pass (no spec/plan doc — direct implementation throughout, consistent with this playground's fast-prototyping purpose), fixing real bugs found via hands-on testing in Expo Go on a physical phone, not just code review or the browser/Playwright workflow. Touches only `apps/playground` (Demo 01–03, `useCardMotion`, `SimpleCard`) — no Batak/Pişti changes.

- **Demo 01 fan-arc smoothness bug, fixed.** `computeFanSlot` (`fanLayout.ts`) previously computed `x` as a pure linear step and `y`/rotation as an independent function of rotation magnitude alone — the two were never tied to one shared geometry, so increasing the Spacing slider widened `x` with no matching change to `y`, exposing the outer cards' top corners. Confirmed both numerically (a reproduction script showing measurable seam growth with spacing) and visually (cropped comparisons against `alper-games-batak.jpeg` and live Batak/gömmeli screenshots — the *exact same* root cause independently affects `apps/mobile/src/table/seating.ts`'s `fanRotationDeg`/`fanCurveY` too, flagged to the user but explicitly not touched this pass, at their choice). Fixed with a "tangent walk": each card steps a fixed distance in the direction its own rotation implies, so `x` and `y` can never diverge regardless of spacing/count/rotation-clamp — verified to produce zero measurable seam across every previously-broken parameter combination tested.
- **Demo 02 selection reworked to match the real game's interaction model.** Previously each card tracked its own independent `selected` boolean (multiple cards could stay lifted at once; switching cards left the old one stuck). Now: one shared `selectedCardId` (exactly one card lifted at a time, tapping a different card switches directly — mirrors `useCardSelection`), select-snaps/deselect-eases asymmetry matching `SelectableCard.tsx` exactly (150ms deselect, 0ms select), and a `DeselectableSurface`-style full-container Pressable so tapping empty space deselects too.
- **Demo 03, several rounds of user-driven fixes**, roughly in this order: one-tap vs. two-tap play mode (live-toggleable, default two-tap); the same tap-outside-to-deselect treatment as Demo 02, widened to the entire screen (an early version only covered the area around the hand); a **z-index bug found and fixed** — an earlier version of this same pass had given a playing/traveling card a dynamically-elevated z-index so rapid simultaneous plays would stay visually unambiguous, but this made a flying card unnaturally pop in front of its still-seated neighbors mid-flight — removed entirely, every card now keeps its own fixed, natural JSX-sibling stacking order for its whole lifecycle; a **Stress Test button** that programmatically fires each card's real `handlePress` (via a per-card registration map, not a special-cased shortcut) with a staggered start and a realistic double-tap gap in two-tap mode, used throughout this pass to exercise multi-card-simultaneous-animation scenarios without needing the device in hand; and a **Rapid Play mode** (off by default) letting a real tap interrupt a card that's still traveling/holding and immediately replay it, instead of being ignored until its ~1.15s cycle finishes on its own.
  - **Touch-target bug found and only partially fixed.** Cards never actually leave the hand array in this demo (they cycle idle→traveling→holding→atRest forever) — but each card's outer `Pressable`'s hit-test box was always anchored at its *original* static layout position, never following the `transform`-only motion (RN transforms never participate in hit-testing — same root cause as `SelectableCard.tsx`'s own documented hitSlop workaround, just the inverse problem). Once a card started flying, its now-invisible-but-still-present hitbox kept winning touches over whichever neighbor was now visibly exposed underneath it. **Fixed via Option A only**: `pointerEvents: 'none'` on a card's Pressable whenever `playStage !== 'atRest'` — confirmed this resolves the reported bug. **Option B (a second, correctly-positioned touch target during the static `holding` phase, to restore Rapid Play's manual re-tap capability) was explicitly scoped, discussed with the user, and deferred pending their confirmation — then never circled back to.** Rapid Play's interrupt-and-replay logic still exists and still fires correctly via the Stress Test button's programmatic `press()` calls, but right now cannot be triggered by a real tap at all, since there's no touch target left on a card once it leaves `atRest`. **Pick this up first if continuing this thread.**
- **`useCardMotion` (`engine/useCardMotion.ts`) rewritten — supersedes its description in the entry above.** This is the shared primitive every demo's card motion depends on, so the fix applies everywhere, not just Demo 03. Went through 3 real iterations chasing a persistent "jump" artifact reported live on a physical phone via Expo Go — not reproducible via the browser/Playwright workflow, native-only:
  1. Removed a permanent `Animated.Value.addListener` that forced a native-to-JS bridge crossing on every single animation frame, for as long as any card was animating, purely to keep a position ref in sync — real cost, but not sufficient on its own.
  2. Replaced it with an on-demand `stopAnimation(callback)` read inside `retarget()` — turned out, confirmed by reading `node_modules/react-native`'s actual `AnimatedValue.js` source directly rather than assuming, that `stopAnimation`'s callback is *still* an async bridge round-trip (`NativeAnimatedAPI.getValue`) for a `useNativeDriver: true` value, so rapid successive retargets (Rapid Play/Stress Test) still queued up real bridge traffic.
  3. **Root cause was architectural, not a performance tuning problem.** The original design normalized every motion leg onto one shared `progress` value running 0→1, resetting it to 0 and swapping `.interpolate()` mappings (via a `generation`-bumped re-render) on every retarget. The reset took effect on native immediately; the new interpolation mapping only took effect once React's re-render committed, a moment later — in that gap, the view could render one real frame through the *old* mapping at input 0, pointing at the wrong (previous leg's start) position: a visible jump on every single select/deselect/travel/hold/reset transition. **Rewritten around `apps/mobile/src/components/SelectableCard.tsx`'s already-proven pattern instead**: one persistent `Animated.Value` per property (x/y/rotate/scale/glyphScale), retargeted via direct `Animated.timing(value, {toValue}).start()` calls — `Animated.timing` always continues from a value's actual current state with no reset/swap step, so there's no gap for a stale frame to render through. **Confirmed fixed on the user's actual phone via Expo Go** — a real, live-device-verified fix, not just a typecheck-clean claim.
  4. Also memoized `SimpleCard` (`React.memo`, benefits every demo) and `PlayableDemoCard` (Demo 03 only), and stabilized Demo 03's per-card `slot` objects via `useMemo` — an unmemoized inline `computeFanSlot(...)` call was silently defeating the new `PlayableDemoCard` memo on every render, same fix pattern as Pişti's `PILE_CARD_OFFSETS`/`SIDE_CARD_STYLES` bug from an earlier session.
- **Reanimated reconsidered and explicitly declined again, this time with direct evidence rather than just the original stated rationale.** The user asked mid-debugging whether to switch to `react-native-reanimated` and rebuild from the ground up; recommended trying the targeted `useCardMotion` fix first, since switching would reopen the deliberate "stay on `Animated` so an eventual Batak port is a drop-in, not a second animation system" decision for no yet-proven necessity. The subsequent fix (item 3 above) resolved the actual reported bug using plain `Animated` — confirmed on-device — which is now concrete evidence, not just intent, that `Animated` is sufficient for this project's needs. Standing recommendation: keep it this way unless something Reanimated-specific (e.g. drag-gesture physics) becomes a real, separate requirement.
- **Session ended before committing** — `SimpleCard.tsx`, `Demo03PlayTravel.tsx`, and `useCardMotion.ts` were left modified/uncommitted in the working tree (`fanLayout.ts`/`Demo02Selection.tsx` from this same pass were already committed separately, in `0ee47f9`, "Demo 3 on animation playground is improved."). **Commit these before further work**, or a future session may lose track of what's actually shipped vs. still local-only.
- **Known, still-unsolved animation problem the playground exists to solve: Batak's trick-center card resize isn't actually smooth — it's worked around, not fixed.** `apps/mobile/src/games/batak/table/TrickCenter.tsx`'s own doc comment (around its `isHumanPending`-branch `TravelCard`) records that an earlier version scaled the human's played card down from `size="normal"` to `size="small"` while it traveled to the trick center, to match the resting trick card's footprint — but `PlayingCard`'s `"small"`/`"normal"` variants have independently-tuned internal proportions (`CORNER_INDEX_WIDTH`, `WATERMARK_ICON_SIZE` in `packages/ui/src/PlayingCard.tsx`), not a uniform scale of one another, so even a perfectly smooth scale interpolation still landed on a visibly different shape at arrival — read as an abrupt "settle" right when the card lands. The shipped fix was to **sidestep the problem, not solve it**: the traveling card now renders at a constant `size="normal"` for the whole flight (no resize at all), which avoids the mismatch but isn't what a real "card shrinks smoothly as it lands" motion should look like. Demo 05 in the Animation Playground (`scale/glyph transformation, single timeline`) is exactly the sub-project meant to work this out in isolation before it's ported back — treat this as one of that playground's essential open jobs, alongside the human-hand travel-origin gap already noted below.

**Demo 6 rail-fan touch-target bug, fixed (2026-07-28).** Every `HandCard`'s `Pressable` shared one identical static box (all real positioning lived in `transform`) — RN hit-testing follows a view's layout frame, never its transform, so every card's touch target was stacked in the same spot and only the last-painted (rightmost) card could ever receive a tap, regardless of where the user actually pressed. Fixed by giving each card a real per-card static box at its own resting rail position (`railPosition(slot.angleDeg, 0)`), and making `useRailCardMotion`'s transform output a *delta* from that origin instead of an absolute position — same visual result (delta + origin = the old absolute position, by construction), but the touch target now tracks the actual card. Also closed a one-frame flash this introduced: when a reflow shifts a card's origin, the static box updates in the same render, but the animated correction previously only landed on the next `requestAnimationFrame` tick; fixed with a synchronous re-projection at the start of `retarget()` plus switching the reflow effect to `useLayoutEffect` so the correction lands before paint.

**Standing animation-quality backlog, not yet solved (2026-07-28): four recurring, related problems keep surfacing on Demo 6, short of the "flawless" bar Demo 4/5 already clear.** Recorded here as a forward-reference checklist so future sessions don't have to rediscover them by ear:
1. **Double-tap mode's deselect-back-to-rest stutters** instead of easing smoothly back into the fan.
2. **The played card's fly animation doesn't read as one single continuous motion** — some seam/discontinuity breaks it into perceptibly separate legs, unlike Demo 3's own travel.
3. **The resize at the end of the fly (landing) isn't smooth** — the same "abrupt settle" problem already known and only worked around (never fixed) in Batak's `apps/mobile/src/games/batak/table/TrickCenter.tsx` (see its own entry above): a scale interpolation alone isn't enough when the two end states' internal proportions genuinely differ.
4. **Reposition (the remaining hand's reflow once a card is played) still isn't one elegant motion** — the cumulative case: reflow quality is entangled with all 3 items above, since the remaining cards' motion has to read as continuous with both the departing card's own animation and the selection/deselect mechanics around it.

**The statement these four items are in service of, for future reference: "The full smooth card playing with rail-like fan layout."** Demo 4 and Demo 5 already prove each underlying mechanism (easing/landing choice, scale/glyph transform) can be flawless in isolation on a single card — the open work is bringing all of Demo 5's mechanisms (or a workaround with an equivalent outcome) onto the rail model in Demo 6, so the whole played-card lifecycle (select → lift → fly → resize → land) and the hand's own reflow both read as one continuous motion, never spring-like or two-part. When item 4 lands seamlessly, come back and update this entry to record that the rail-fan animation infrastructure is solid against this statement — that update is the real completion signal for this backlog, not any individual item.

**Root-cause fixes attempted for all 4 items above (2026-07-28), pending the user's live confirmation before this backlog is considered closed.** All four traced back to two real, separate bugs rather than four independent problems:
- **The motion primitive itself.** `useRailCardMotion` (the hook the previous entry's touch-target fix still describes) drove every HandCard's position via `requestAnimationFrame` — a per-frame JS-thread loop doing `Math.sin`/`Math.cos` and `Animated.Value.setValue()` by hand, needed only because deriving x/y from a single angle requires real trig that `.interpolate()` (piecewise-linear only) can't produce. JS-thread rAF stepping is inherently more stutter-prone than native `Animated.timing`, and this was very likely why deselect (item 1) and reflow (item 4) stuttered. **Replaced entirely**: `HandCardComponent` now uses the same `useCardMotion` primitive Demo 2/3/5 already prove is stutter-free (`Animated.timing` per property, `useNativeDriver: true`), treating each card's rail position as a plain `(x, y, rotateDeg)` keyframe computed via `railPosition()`. The one real tradeoff, made deliberately and flagged rather than hidden: a mid-reflow transition now eases x/y independently (a straight chord between the old and new rail position) instead of literally curving along the arc — every card still starts and lands exactly on the rail (the geometry itself, `railAngles`/`railAngleStepDeg`/`railFanWidth`, is untouched), and the chord is brief/short enough (~320ms, a few degrees) to read as equivalent. `useRailCardMotion.ts` deleted as dead code once nothing referenced it.
- **Two real, separate discontinuity bugs on the played card, both root-causing item 2 (and compounding item 3):** (a) `PlayedCard` computed its mount position from `railPosition(departureAngleDeg)` alone, ignoring the select-lift — so a card played from the lifted (two-tap) position would instantly snap down to its unlifted resting spot before flying up, a real visible seam. Fixed by threading a `wasSelected` flag from `HandCardComponent.handlePress` through `onPlay`/`playedCard` state into `PlayedCard`, which now mounts at `railPosition(departureAngleDeg, wasSelected ? SELECT_LIFT_PX : 0)` — wherever the card actually was. (b) `PlayedCard`'s `motion.retarget(...)` call (which drives both the fly AND the scale-down together, already a single timeline exactly like Demo 5's own mechanism) lived in a plain `useEffect` — since this component mounts fresh on every play (unlike Demo 3/5's persistent card, which calls `retarget()` synchronously from an event handler on an already-mounted instance), a passive effect lets one real frame paint the static "at rest" keyframe before the animation actually starts: a pause-then-jump on both position and scale. Fixed by switching to `useLayoutEffect`, so the very first painted frame is already mid-motion.
- Both `useCardMotion.jumpTo`'s reprojection trick (used identically to the previous touch-target fix, just now driving `useCardMotion` instead of `useRailCardMotion`) and the `useLayoutEffect` timing fix reuse patterns already proven elsewhere in this codebase (`useCardMotion.ts`'s own 2026-07-26 jump-artifact fix; `jumpTo`'s doc comment literally names "Demo 06's hand reflow" as its intended use case). `npx tsc --noEmit` clean on `apps/playground` after every step. **Not yet live-tested** — per the user's 2026-07-17 direction, no proactive screenshot/Playwright verification was done; this needs the user's own on-device/browser test before being called fixed. Once confirmed, update this entry (not a new one) to record the "full smooth card playing with rail-like fan layout" statement as achieved.

**Two more real bugs found via the user's own on-device test of the fix above (2026-07-28), both fixed, both distinct from anything already described:**
- **Reflow only eased when playing the rightmost card; every other card snapped instantly.** Root cause: the render still split remaining cards across two separate JSX parent `View`s (one before the played card, one after) purely to make the departing card stack correctly between its former neighbors. React reconciles children by `key` only *within one parent's array* — a card moving from one parent's children array to the other (which happens to every card whose before/after group shifts as the split point changes) is unmounted from the old parent and mounted fresh in the new one, discarding its `useCardMotion` Animated.Value state and snapping instead of easing. Playing the *rightmost* card never needs to move any remaining card into the other group (nothing was ever "after" it), so that specific case looked fine, masking the bug everywhere else. Fixed by rendering every remaining card as a direct sibling under one shared parent (stable `key`-based identity for its whole life in the hand) and controlling stacking purely via the `zIndex` style: each `HandCard`'s `zIndex` is its own current array index, and `PlayedCard`'s is `originalIndex + 0.5` — strictly between whatever its former left/right neighbors' own zIndex values are, with no JSX reparenting involved at all.
- **The played card visibly popped sideways before flying.** `PlayedCard`'s mount/landing X math (`centerXAtPlay + origin.x`) never subtracted `SIMPLE_CARD_WIDTH / 2` the way `HandCard`'s own Pressable already does — `railPosition().x` is a center-relative coordinate, and `HandCard` correctly converts it to a left-edge position, but `PlayedCard` didn't, so it mounted (and landed) half a card-width too far right relative to where the card actually was and where "center" actually is. Fixed by applying the same subtraction in both places. This had been present since the very first rail-model implementation, not introduced by any of the fixes above.
- Both fixes are pure position/z-order corrections, no new tradeoffs. `npx tsc --noEmit` clean. Still pending the user's own live confirmation before any of this backlog is marked closed.

**Next up:** ask the user what to tackle next. Real candidates: (1) Demo 03's Rapid Play touch-target gap noted immediately above (Option B, deferred) — cheapest, most immediately actionable; (2) the Batak trick-center smooth-resize problem noted immediately above — genuinely unsolved (only worked around), and squarely what the Animation Playground (Demo 05 especially) was built to figure out; (3) investigate and fix the Batak bidding-AI miscalibration noted above — still empirically confirmed at both 4-player (49/200, at chance) and 3-player gömmeli (68% for Hard) rather than just anecdotal, arguably the most urgent of the Batak-side items since it affects the already-shipped 4-player game and still blocks restoring `simulate.test.ts`'s original assertion strength; (4) the human hand's card-travel-origin gap noted above (fixed generic offset vs. the card's real position) — still not decided/spec'd, though the animation-playground work above may now make this cheaper to port; (5) the "eşli" (partnered) Batak variant, not started at any layer; (6) moving on to Klondike Solitaire per the original roadmap order. The Batak UI's visual polish against the Alper Games reference checklist Pişti went through (distinct from the 7-item pass completed earlier) remains explicitly deferred and not yet scheduled — and now that gömmeli has its own UI, whether to bring any of that checklist's items to gömmeli specifically is also an open, unscheduled question. Also open: whether to port this pass's fan-arc-smoothness fix (Demo 01, above) into Batak/Pişti's own `seating.ts`, which has the identical bug — raised with the user, explicitly not decided.

Known follow-up items deferred from Phase 1 (non-blocking, noted in the final whole-branch review):
- `RuleEngine.setup(options: unknown, ...)` loses type safety across the options-passing chain — consider a `TOptions` generic once real games multiply.
- `GameState.rngState` exists for reproducible resume but still isn't exercised by mid-game randomness: Pişti's `setup` pre-shuffles the whole deck up front, and its mid-hand redeals just deal from the already-ordered `stock` zone, consuming no further RNG. This remains open for a future game whose mid-game randomness is genuinely re-rolled (e.g. a game that reshuffles a discard pile back into a live deck).
