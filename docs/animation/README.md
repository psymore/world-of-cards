# Animation Documentation

The entry point for this folder is **[`00-DocumentationMap.md`](./00-DocumentationMap.md)** — it explains what every document here is for, the authority relationship between them, and the order to read them in.

This file exists only so the folder still shows something useful when browsed directly (GitHub, for example, auto-displays `README.md` for a folder but not other filenames). All real content lives in `00-DocumentationMap.md`.
